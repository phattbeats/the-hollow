# state.md - i18n Scaling cross-phase cheat sheet

The single source of truth a fresh session reads before doing anything. Record decisions once here; reference forever. Update at the end of every phase.

---

## Current status
- **PACKET COMPLETE + Phase 9 QA DONE (PASS-WITH-FOLLOWUPS, 2026-06-16):** all 9 phases delivered
  and QA'd; this was the FINAL whole-packet QA. 6-lane read-only coverage Workflow (correctness,
  test-coverage, dead-code, privacy-security, cross-platform-sync, qa-checklist) + main-loop
  deterministic gates + adversarial verify: **0 BLOCKING / 1 HIGH (downgraded MEDIUM, FIXED) / 4
  LOW / 15 INFO;** neither stopping rule tripped. ONE fix (commit `590d7b3`, explicit path):
  `tests/i18n_pseudo_locale.test.ts` now asserts index.html references neither `en_XA` nor `en-XA`
  (the hreflang test was presence-only; the pseudo file's header claimed hreflang coverage it did
  not deliver) - regression-prevention, index.html is clean today. Gates: `tsc` clean; PR tier
  `npm test` 131 files **1331 pass / 7 skip**; release tier (`I18N_RELEASE_TIER=1`) fails on EXACTLY
  the 2 intended Phase 8 checks (registry empty-pending=456 + admin H3b copied-English), ZERO
  collateral / ZERO Phase 9 regression - the two-tier gate correctly holding the deferred 14-locale
  admin fill; `build:env`/`build:server`/`build` exit 0; reproducibility EXACT (tree clean after
  regen, both resolved tables + status.json + media manifest byte-identical); game gate SHA UNCHANGED
  (`d9db528..`, 1,584,856 B); bundle unchanged (1,121.05 / 33.17 KB gzip); en_XA PROD tree-shake
  PROVEN (0 pseudo-only glyphs `ɱ ʋ ɋ ẋ ĝ ĥ` in both dist bundles); copy review clean. All four
  en_XA exclusions (supportedLanguages, hreflang, release gate, player+operator picker) re-verified
  directly in source. Whole-feature QA matrix (`qa-checklist.md`) all rows PASS (deploy verify N/A).
  DEFERRED FOLLOW-UPS (surface before teardown, do NOT lose): (1) the en_XA-surfaced hard-coded
  literals - minimap side-button aria-labels `hud.ts:1386-1392`, Skin/Chroma swatch aria-labels
  `hud.ts:3688`/`3756` + `index.html:4653`/`4692`, chat tip `hud.ts:451`, version-footer "build"
  `main.ts:180`; (2) OPEN RFC 9.6 release-fill ownership / API key; (3) the admin chat-filter
  14-locale fill (456 pending rows) the release gate is correctly blocking; (4) LOW: admin
  `tableFor()` omits the game's `lang === current` guard (benign today, optional one-line hardening).
  TEARDOWN of `docs/i18n-scaling/` is GATED on explicit user confirmation (not done yet). Next:
  Phase 9 (delivery) details below.
- **Active phase:** **Phase 9 DONE (en_XA dev-only pseudo-locale, 2026-06-16)**: shared deterministic accent+bracket transform (`scripts/i18n_pseudo.mjs`, fixed 1:1 ASCII->Latin map, every leaf bracketed, any `{...}` token preserved EXACTLY, emoji/non-ASCII pass through) emitted by BOTH build scripts as a separate `en_XA` export that is NOT a member of `translations` -> excluded BY CONSTRUCTION from supportedLanguages/the language picker/index.html hreflang/the release gate/the registry. Selectable via `?lang=en_XA` on NON-RELEASE builds only (game `src/ui/i18n.ts` + admin `src/admin/i18n.ts`; a `tableFor()` indirection swaps the pseudo table onto the current-language read path, gated by `!isReleaseBuild()`, base locale stays the real `"en"`, never persisted). The en_XA reference sits behind a static `import.meta.env.PROD` guard so a production `vite build` TREE-SHAKES the pseudo table out of BOTH bundles (verified: 0 signature glyphs in `dist/assets/main-*.js`+`admin-*.js`; sizes unchanged 1,121.05/33.17 KB gzip). Game gate SHA UNCHANGED (`d9db528..`, 1,584,856 B - the hash reads `supportedLanguages`, which omits en_XA). `tests/i18n_pseudo_locale.test.ts` (12: parity/bracketing/placeholder/accent/determinism + exclusion + dev-on/release-off) + tsc + named suite + `npm run build` all green; generated tables deterministic + purely additive. 3 coverage reviews (privacy-security [YES the gate blocks prod, no secret/flag leak, no XSS sink], cross-platform-sync [selector parity, sim/server untouched, byte-equivalence re-verified], correctness [new test mutation-proven to bite]) all NON-BLOCKING (0 BLOCKING/HIGH/MEDIUM). en_XA PAID FOR ITSELF on first run: a live `?lang=en_XA` browser scan (login/HUD/6 windows/options/admin-login) surfaced real hard-coded literals the type system + S3 guard both miss - minimap side-button aria-labels (`hud.ts:1386-1392`), Skin/Chroma swatch aria-labels (`hud.ts:3688`/`3756`, `index.html:4653`/`4692`), the chat tip literal (`hud.ts:451`), the version-footer word "build" (`main.ts:180`) - all recorded as DEFERRED follow-ups (fixing them is OUT of Phase 9 scope). Scope notes: en_XA pseudo-izes only the MAIN+admin `t()` tables (matcher-channel sim/server text resolves via `DICT[getLanguage()]`=en under pseudo, so it stays plain English - NOT a literal; canvas/FCT text is non-DOM; the admin scan covered the login screen only). Next up Phase 9 QA + teardown (optional). Earlier: Phase 8 DONE + **Phase 8 QA DONE (PASS WITH FIXES, 2026-06-16)**: 6-lane coverage Workflow (correctness, test-coverage, dead-code, privacy-security [MANDATORY], cross-platform-sync, qa-checklist) + main-loop deterministic gates; 0 BLOCKING / 0 HIGH, neither stopping rule tripped. PROVEN DIRECTLY: 0 completeness regression (all 181 existing admin keys x 14 locales byte-identical to the pre-Phase-8 dense DICT at `77a15ac`; +38 new keys -> 219; 0 resolution gaps); the release gate bites on EXACTLY the 2 intended checks (registry `pending`=456 + admin H3b copied-English `nav.chatFilter`) with no collateral (106 pass); the built admin chunk has 0 game-locale markers (`abilityUi`/`classDetails`/`entities.*`); the game baseline `d9db528..` is untouched (recomputed 1,584,856 B); RFC 9.7 holds (admin.html 18/18 `data-i18n` keys are real admin en keys; index.html `data-i18n-content` meta is game-side; no Postgres/DDL change in the 28-file diff). FIXES (2 commits, explicit paths): recursed the bundle-isolation test into `i18n.locales/` (`b9cc7cd`; mutation-proven to bite a smuggled `../../ui/i18n.resolved.generated` import in an overlay); corrected the stale `i18n_scan.mjs` header (`7b3e195`; registry regenerates byte-identical). DEFERRED (non-blocking): admin date formatters (`fmtDate` + the four `dialog.until` rows) render in the HOST default locale not the operator's selected admin locale (cosmetic + PRE-EXISTING - `fmtDate` already did this, so it is NOT an i18n-rule violation; optional follow-up to thread `adminLanguage()` as the locale arg); an admin hardcoded-string source-scan guard (Phase 9 `en_XA` pseudo-locale is purpose-built to surface raw literals, incl. admin); `adminLanguage()` unused getter (kept as public-API symmetry with `setAdminLanguage`). privacy-security PASS (auth/route/token guards untouched, escapeHtml 33->33 / 4->4, release `t()` fail-closed before interpolate; no secret/PII in new strings or the generated table). Next up Phase 9 (pseudo-locale, optional). Earlier: Phase 7 DONE (2026-06-16): release-fill worklist tooling + docs. `scripts/i18n_fill_worklist.mjs` (`npm run i18n:worklist`) turns the registry's `pending` slice into a per-language `{scope,key,english,placeholders,siblings}` delta under the GITIGNORED `docs/i18n-scaling/worklist/`, splitting `autoFillable` (mechanical chrome) from `humanRequired` (prose, blocked-by-default), and ships the hand-maintained locked-terms glossary (`scripts/i18n_glossary.json`: brand verbatim + 9 classes + ability/zone/dungeon `*.name`, each resolved to its established per-locale form) in EVERY batch. DATA-ONLY (no network/LLM/Date.now/Math.random; esbuild `data:`-URL load like the scanner), deterministic, content-hash-cached zero-write no-op on an unchanged repo; a staleness guard refuses a registry that drifted from source. Round-trip PROVEN (depopulate chrome+prose de_DE keys -> scan -> worklist routes chrome to auto/prose to human -> fill -> pending shrinks -> restore -> registry byte-identical, stale batch auto-removed). Contributor flow rewritten in `src/ui/CLAUDE.md` (the stale "add to every locale object" text is gone); maintainer flow + the OPEN RFC 9.6 ownership/API-key item in `docs/i18n-scaling/translation-workflow.md`; `tests/i18n_fill_worklist.test.ts` (10) locks the stopping rule. Blocked-surface list FINALIZED (see Glossary section). Resolved-table baseline UNTOUCHED (`d9db528..`: tooling only, no depopulation, `pending` still 0). tsc + named suite (79 pass/5 release-tier skips) + build all green; 3 coverage reviews (privacy-security, cross-platform-sync, correctness) all NON-BLOCKING. **Phase 7 QA DONE (PASS WITH FIXES, 2026-06-16):** 6-lane read-only coverage Workflow + main-loop stopping-rule proofs; 0 BLOCKING, 2 HIGH + 2 MEDIUM + 1 LOW fixed, 2 LOW/INFO deferred. Re-proven directly: DATA-ONLY, deterministic no-op (empty `25e8af71`, non-empty `54d42499`), full round-trip (chrome->autoFillable, prose->humanRequired), STOPPING RULE across the full 1925-key en space (0 keys both fillable AND prose-prefixed), glossary in every batch. FIXES: extracted a PURE assembly core (`buildWorklistOutputs`/`assertAutoFillableHasNoProse`; byte-identical output, hashes unchanged) + 4 fixture tests (`tests/i18n_fill_worklist.test.ts` now 14: determinism + inputHash teeth + end-to-end prose segregation + glossary/dialect + the belt-and-suspenders throw); reworded the sim/server matcher guidance to ORIGIN-based (the S3 guard accepts recognition by EITHER matcher, not a forced sim+server mirror) in `translation-workflow.md` + `src/ui/CLAUDE.md`; noted the glossary is ADVISORY (brand-in-chrome unenforced) + the `--lang` clobber caveat; fixed one Phase-7 em dash in `src/ui/CLAUDE.md`. tsc + named suite (83 pass/5 release-tier skips) + build all green; resolved-table baseline UNTOUCHED (`d9db528..`). DEFERRED: the `DIALECT_BASE`/`isPresent`/`sourceModule` 3-script duplication (shared-constants extraction touches the out-of-scope build+scan scripts) + an automated glossary brand-preservation guard (fill pipeline, Phase 8+). RFC 9.6 ownership/API-key STILL OPEN. Next up Phase 8 (admin catalog). Earlier: Phase 6 DONE (2026-06-16): THE UNLOCK - English-only PRs are now legal. Overlays are sparse-legal (`Partial<Record<TranslationKey,string>>`; `TranslationKey` deepened to `Leaves<typeof en,6>`, no tsc cost); `t()` throws on an untracked key in dev/test (raw key on release), renders English for a registry-`pending` key on **non-release builds only**, and HARD-FAILS a pending key on release (flag `isReleaseBuild()` = `I18N_RELEASE=1` OR `import.meta.env.PROD`). CI split by git ref: `pr-gate` (no tier flag) vs `release-gate` (`I18N_RELEASE_TIER=1`). S3 split into `s3_registered`(PR)/`s3_localized`(release, 14 locales); H3b + coverage quest/talent copied-English + empty-`pending` (registry + t-behavior) are release-only. Resolved-table byte-equivalence UNCHANGED (`d9db528..`): overlays stay DENSE (NOT depopulated), so `pending`=0 and the table is byte-identical; the pending English-fill is WIRED-but-DORMANT until Phase 7 depopulates - so `src/ui/i18n.resolved.sha256` was deliberately NOT moved (the phase-doc STEP-3 baseline-move is conditional on actual depopulation). **Phase 6 QA DONE (PASS WITH FIXES, 2026-06-16):** all 4 gate cases RE-PROVEN directly in the main loop (untracked key throws in dev/test AND fails the PR gate via tsc `TS2345`; pending renders English on non-release only; a pending key BLOCKS the release gate - dropping `es."nav.home"` failed 6 release-tier gates and moved the hash to `524d95f4..`, restored; unregistered+untranslated fails the PR gate) + 6 coverage lanes incl. the MANDATORY privacy-security-review (NON-BLOCKING, all 3 sub-checks PASS: t() throws pending before interpolate on release, no English fallback, fail-closed release flag). 0 BLOCKING/HIGH; 1 MEDIUM fixed (commit `79d0732`: the empty-pending release gate now has an ASSERTED teeth test via a shared `assertNoPending()` helper, proven biting by mutation); 2 LOW deferred (`workflow_dispatch` on `release/**` -> pr-gate is documented-intended in ci.yml, left as-is; stale deleted-dense-test refs in the DORMANT out-of-range `scripts/i18n_flatten_locales.mjs`). Baseline `d9db528..` confirmed unchanged-correct (overlays dense => pending=0 => byte-identical). PRIVACY RESIDUAL for Phase 7 (not blocking, dormant while PENDING_TOTAL===0): prod release semantics rest SOLELY on `import.meta.env.PROD` under `vite build`; no `I18N_RELEASE=1` in any deploy/Docker/CI env - set it as defense-in-depth before the first real pending key ships. Next up Phase 7. Earlier: Phase 5 DONE + Phase 5 QA DONE (PASS, 2026-06-16): status registry + no-LLM/no-network scanner. `scripts/i18n_scan.mjs` walks the full key universe (en leaves + sim/server/admin DICT keys), emits `src/ui/i18n.status.json` (2278 keys x 13 non-en locales; `translated`/`pending`/`blocked` + `srcHash`/`enHash`/`by`; pending=0 because still dense). The two former hand-maintained allow-list Sets (`COPIED_ALLOW` 43, `ALLOW_V07_SLASH` 105) are now VIEWS over the registry. Scanner is a PURE function of source (no prior-output read) -> reproducible; `i18n:scan` wired into `pretest` + `build` after `i18n:build`. Resolved-table byte-equivalence unchanged (SHA `d9db528..`). Phase 5 QA verdict PASS (6 lanes + adversarial verify: 0 BLOCKING/HIGH/MEDIUM, 2 LOW deferred, 33 INFO; NO code change needed - every load-bearing property already has a test that empirically bites, proven by 5 force-mutation experiments + an independent zero-divergence re-derivation of all 29614 rows). Two deferred LOWs (both already guarded, revisit in Phase 6): (1) the registry-in-sync test does NOT catch an overlay key absent from `en` (scanner enumerates `main` by en leaves) - that case is guarded by the Phase 3 `i18n_overlay_key_membership.test.ts`, so the QA doc's attribution to the registry test is a doc nuance, not a gap; (2) the scanner's seed-rot `throw` + the `pending` branch are only indirectly asserted (the throw is hard-gated by the scanner in `pretest`; `pending` is empty until sparseness lands). Earlier: Phase 4 DONE + Phase 4 QA DONE (PASS, 2026-06-16); Phase 2 QA also DONE (PASS, 2026-06-16, run in the same session per the user's request to run both QA files). Phase 4 deduped the three dialect pairs to divergence-only overlays (es_ES=72 keys over es, fr_CA=37 over fr_FR, en_CA=3 over en) behind a data-driven `DIALECT_BASE` merge in `scripts/i18n_build.mjs`, removed the `{} as WorldEntityTranslations` casts (deleted the dead non-English data in `world_entity_i18n.ts`, now English-only) and the `{} as TalentLocaleText` casts (explicit aliases in `talent_i18n.ts`). Resolved table byte-identical (SHA `d9db528..`). Phase 2/4 QA each landed one new test (commits `0d49b25` Phase 2 gap-fill, `e20700f` Phase 4 dialect resolution); see progress.md QA checklists. Phase 3 + Phase 3 QA DONE (PASS): permanent subset guard `tests/i18n_overlay_key_membership.test.ts` (commit `2c2066f`) + one-shot migration comment fix (commit `c5f6738`). Phase 2 DONE (commits `3f1ed8d`, `a92ff37`, `ffb40e5`). Phase 1 DONE + QA PASS.
- **Branch:** `refactor/i18n-phase-naming` (Phase 0 - the `phaseN`->content-name rename - is DONE and lives here). Later phases may branch `feature/i18n-<slug>` off this; confirm with the user before branching (shared worktree).
- **RFC:** `docs/design/i18n-translation-scaling.md` - the authoritative design. This packet implements it with the four decisions below applied.

## Locked design decisions (maintainer sign-off, 2026-06-16)
1. **Two-tier CI gate.** CI splits by git ref into a cheap PR gate and a full release gate. An English-only PR is legal once Phase 6 lands.
2. **Dense generated artifact keeps `tsc` safety.** A no-dep `.mjs` build script overlays sparse locales onto `en`, fills gaps from English, and emits `src/ui/i18n.resolved.generated.ts` typed `: typeof en`. Client and admin import the generated artifact. Reproducibility-checked (`git diff --exit-code`) like the media manifest.
3. **Flat dotted-key overlays for the 13 non-English locales.** `en` stays a **nested** object (authoritative base; drives `TranslationKey = Leaves<typeof en>` that ~3,532 call sites use). Each non-English locale is a flat `Partial<Record<TranslationKey, string>>` containing only translated keys. `en_CA` is a thin alias of `en`; `es_ES`/`fr_CA` are dialect overlays over `es`/`fr_FR`.
4. **`t()` on miss:** throw on an *untracked* key in dev/test; render English for a registry-`pending` key on **non-release builds only**; release builds require an empty `pending` set. This deliberately relaxes "the rendered text always comes from `t()`" for the dev/pre-release window only - a non-release build carrying `pending` keys must never be deployed.
5. **Declared-base dialect inheritance (Phase 4).** A dialect locale declares a base locale via the data-driven `DIALECT_BASE` map in `scripts/i18n_build.mjs` (`es_ES`->`es`, `fr_CA`->`fr_FR`, `en_CA`->`en`). The build resolves a dialect as nested `en` -> base-locale overlay -> dialect overlay, so the dialect overlay carries ONLY the keys whose value diverges from the base; an omitted key falls through to the base, then to English. The dialect graph lives in that one map, not in scattered per-locale branching. This is the single dialect-inheritance mechanism for the resolved table (the `{} as` casts that previously faked it in `world_entity_i18n.ts`/`talent_i18n.ts` are gone).

### Operational decisions (RFC §9.5-9.7)
- **LLM fill scope (locked):** worklist generation is automated; **prose is blocked-by-default** - quest narratives, class/ability names, and CJK talent names are `blocked: human-required` and never auto-filled. A bot may fill mechanical UI chrome only. The human owns the blocked-surface list.
- **OPEN - release fill ownership / API key (RFC §9.6):** who runs the release-time fill pass and owns the key? Bus factor must be acceptable; the worklist must be plain enough for a second maintainer. Not a code blocker; resolve before the first real release fill.
- **Non-client consumer audit (RFC §9.7):** Phase 6 + Phase 8 QA must confirm nothing outside the client seam (server-rendered HTML, `document.title`, `index.html` hreflang tags, the admin DICT, any `data-i18n-content` meta) can surface a `pending`-English string to a real user, and that the `en_XA` pseudo-locale (Phase 9) is excluded from `supportedLanguages` and hreflang.

## Non-negotiable invariants (carry into every phase)
- **`src/sim/` and `server/` stay language-agnostic** - no `t()`, no DOM. They emit English; the client matchers (`src/ui/sim_i18n.ts`, `src/ui/server_i18n.ts`, plus the `hud.ts`-local maps) re-localize at the boundary. Translation resolves only at the client boundary.
- **Determinism is untouched.** This is a UI/build/CI refactor; do not introduce `Math.random`/`Date.now`/`performance.now` anywhere in `src/sim/`. (The scanner/build scripts run at build time, not in the sim, so hashing there is fine.)
- **No new runtime dependency, no i18n framework.** Plain TypeScript data + `.mjs` build scripts, consistent with `scripts/build_media_manifest.mjs`.
- **Generated files are never hand-edited.** `i18n.resolved.generated.ts` and `i18n.status.json` carry the do-not-edit banner and are reproducibility-checked.
- **No em dashes or emojis** in player-facing copy or in these docs' shipped strings.
- **Shared worktree:** stage explicit paths, never `git add -A`. A concurrent session may share this checkout.

## The byte-equivalence safety net (Phases 1-5)
Every behavior-preserving phase is gated by **byte-equivalence of the resolved 14-locale table**. Phase 0 proved the method (the resolved table is deterministic and serializes reproducibly). The current baseline (captured in Phase 1) is **SHA-256 `d9db528bea1c7a1e02835c4d3edb3fabcee3687aad2186608f1f1d2ac83b3b9b`, 1,584,856 bytes**.
- NOTE on the byte count: Phase 0 measured 1,583,881 bytes; the current tree is 1,584,856 because commit `1c751c4` (`feat(auth): Cloudflare Turnstile`) plus a `main` merge added auth/registration i18n keys to `i18n.ts` AFTER Phase 0's snapshot. This is legitimate content, not a refactor drift. The authoritative gate is the committed `src/ui/i18n.resolved.sha256`, not the byte figure - quote the SHA, not the count.
- The mechanism for this packet (built in Phase 1):
- A script `scripts/i18n_resolved_hash.mjs` (zero deps; esbuild-bundles `src/ui/i18n.ts` like `export_loot_spreadsheet.mjs`, reassembles `translations` from the locale exports + `supportedLanguages`) serializes it deterministically with **recursive key sort** (so the hash is insertion-order-independent) and prints/`--check`s/`--write`s its SHA-256. `npm run i18n:hash` prints it.
- A committed baseline `src/ui/i18n.resolved.sha256` holds the expected hash.
- A test `tests/i18n_resolved_equivalence.test.ts` runs the script via subprocess and asserts equality.
- **The baseline hash changes ONLY in phases that deliberately change resolved output (Phase 6 onward, when `pending` keys begin to English-fill).** Phases 1-5 must leave it byte-identical. If a phase needs to touch the baseline, that is a red flag - stop and confirm the change is intended.

## Validation matrix by change type
- **Behavior-preserving structural phase (1-5):** `npx tsc --noEmit` + `npx vitest run tests/localization_fixes.test.ts tests/localization_coverage.test.ts tests/server_i18n.test.ts tests/i18n_resolved_equivalence.test.ts` + the resolved-table byte-equivalence gate (hash unchanged).
- **Generated-artifact phase (2, 5):** also the reproducibility check - regenerate, then `git diff --exit-code` on `i18n.resolved.generated.ts` / `i18n.status.json` must be clean.
- **CI / gate phase (6):** the two-tier split itself must be tested: a deliberately English-only sample key passes the PR tier; a deliberately incomplete locale fails the release tier; the full localization suite passes at the release tier.
- **Admin phase (8):** admin builds and renders all 14 locales; `npm run build` (admin entry) clean; admin registry-in-sync.
- **Full-stack / pre-merge (every phase before declaring done):** `npm test && npx tsc --noEmit && npm run build:env && npm run build:server && npm run build` (mirrors `.github/workflows/ci.yml`).
- **Bundle-size check (where relevant):** note the gzipped size of the main bundle's locale payload before/after; the dense artifact must not balloon it (it replaces, not adds to, today's eager 14-locale table).

## Key file paths
### Existing (today)
- `src/ui/i18n.ts` (~13.2k lines) - types, `en`, 13 full `: typeof en` locales, assembler (`shellStrings`/`hudStrings`/`abilityStrings`/`questStrings`/`itemStrings`/`classAbilityNames`/`itemNames`/`worldNames`/`merge*`), `t()`, `tOptional`, `hasTranslation`, `translationValue`, `formatNumber`/`formatMoney`/`formatDateTime`/`moneyParts`, `getLanguage`/`setLanguage`/`isSupportedLanguage`, `supportedLanguages`, `TranslationKey`/`Leaves`.
- `src/ui/world_entity_i18n.ts` (~260 KB) - class/ability/item/mob/NPC/quest/zone/dungeon/POI names + narratives. NOTE: `es_ES`/`fr_CA` are already aliased to `es`/`fr_FR` at module load via `{} as WorldEntityTranslations` casts.
- `src/ui/talent_i18n.ts` (~187 KB) - talent name overrides + description rules.
- `src/ui/entity_i18n.ts` (~297 lines) - runtime entity resolver (`tEntity`); not a key source.
- `src/ui/sim_i18n.ts` (~1,516 lines) - `enTable` + `BASE_DICT` + `PET_DICT` -> `DICT`; EXACT map (auto-built) + ~28 regex RULES; `localizeSimText()`/`tSim()`.
- `src/ui/server_i18n.ts` (~129 lines) - inlined `DICT` + EXACT + ~37 regex RULES; `localizeServerText()`/`tServer()`.
- `src/ui/hud.ts` - `localizeErrorText` (~L2677), `localizeSystemText` (~L2783), `localizeLootText` (~L2867): hud-local EXACT maps + regex chains that run BEFORE delegating to `localizeSimText`/`localizeServerText`. The matcher-coverage surface includes these.
- `src/admin/i18n.ts` (~137 lines, post-Phase-8) - thin admin runtime: imports the dense resolved admin table, re-exports `DICT` (unchanged consumer surface), `t()` with the pending/release/untracked policy, `classLabel()`/`zoneLabel()`/`localizeAdminError()`. The admin catalog now lives as `src/admin/i18n.en.ts` (flat en base, 219 keys) + 13 sparse `src/admin/i18n.locales/<lang>.ts` overlays -> generated `src/admin/i18n.resolved.generated.ts` (14 locales x 219 keys), built by `scripts/i18n_admin_build.mjs` (`npm run i18n:admin`). Separate `admin.html` bundle. (Phase 8 moved it under the overlay+registry+release-gate model; pre-Phase-8 it was a flat dense `Record<locale, Record<key,string>>`, 181 keys.)
- `index.html` - 185 `data-i18n*` attributes, 14 hreflang links, OG/Twitter/JSON-LD meta, `data-i18n-content`.
- `admin.html` - static `<title>`, meta; admin reads locale from `?lang=`/localStorage at runtime.
- `tests/localization_fixes.test.ts` (~669 lines) - B1, L3/L4, H1/H1b/H2, M1/M1b/M1c, H3/H3b, H4b, S1/S2, R1, A1, **S3** (the drift guard that scrapes `sim.ts`/`hud.ts` source, `de_DE`-only today). `COPIED_ALLOW` (~43) and `ALLOW_V07_SLASH` (~105) literal Sets live here.
- `tests/localization_coverage.test.ts` - 14-locale key parity, rendered quest/talent content, accents, CJK-no-Latin, hreflang.
- `tests/server_i18n.test.ts` - server DICT parity + copied-English.
- `.github/workflows/ci.yml` - single `build` job on PR + push to `main`/`dev-*`/`release/**`. `npm test` runs the full localization suite on every push.
- `scripts/build_media_manifest.mjs` - the zero-dep generated-artifact pattern to copy.
- `scripts/localization_e2e.mjs` - 14-locale E2E (homepage/mobile/in-game/a11y).

### New (created by this packet - see per-phase files for exactly when)
- `src/ui/i18n.en.ts` - authoritative nested `en` + `Leaves`/`TranslationKey`/`DeepPartial` machinery. (Phase 1)
- `src/ui/i18n.locales/<lang>.ts` - created NESTED `: typeof en` in Phase 1 (the 13 non-English locale objects, behavior-preserving). Phase 3 converts them to flat `Partial<Record<TranslationKey,string>>` sparse overlays; the only files a translator edits.
- `src/ui/i18n.resolved.generated.ts` - generated dense `: typeof en` table; client + admin import this. (Phase 2)
- `src/ui/i18n.status.json` - generated registry: `translated`/`pending`/`blocked` + `srcHash` + `by`. (Phase 5)
- `src/ui/i18n.resolved.sha256` - committed byte-equivalence baseline. (Phase 1)
- `scripts/i18n_build.mjs` - overlays + emits the resolved artifact. (Phase 2)
- `scripts/i18n_resolved_hash.mjs` - deterministic SHA-256 of the resolved table. (Phase 1)
- `scripts/i18n_scan.mjs` - no-LLM/no-network scanner that rebuilds the registry. (Phase 5)
- `scripts/i18n_hash.mjs` - side-effect-free content-hash helper (`contentHash`/`placeholdersOf`), shared by the scanner and the registry test. (Phase 5)
- `scripts/i18n_blocked_seed.mjs` - canonical blocked-surface seed (`COPIED_ALLOW_IDS` 43, `V07_SLASH` 105); byte-faithful one-time extraction of the former in-test Sets, now hand-maintained here. (Phase 5)
- `tests/i18n_status_registry.test.ts` - registry-in-sync + reproducibility + hash-sensitivity net. (Phase 5)
- `scripts/i18n_fill_worklist.mjs` - emits the per-language `pending` delta for the release fill. (Phase 7)
- `tests/i18n_resolved_equivalence.test.ts` - byte-equivalence + reproducibility test. (Phase 1/2)

## New package.json scripts (added across phases)
- `i18n:build` -> `node scripts/i18n_build.mjs` (Phase 2; folded into `npm run build` + `pretest`)
- `i18n:hash` -> `node scripts/i18n_resolved_hash.mjs` (Phase 1)
- `i18n:scan` -> `node scripts/i18n_scan.mjs` (Phase 5; folded into `pretest` + `build`, both AFTER `i18n:build` since the scanner bundles `sim_i18n` -> the resolved table)
- `i18n:worklist` -> `node scripts/i18n_fill_worklist.mjs` (Phase 7)

## Per-phase additions log (fill in as phases complete)
| Phase | New files | New scripts | New tests | Notes |
|---|---|---|---|---|
| 1 | `src/ui/i18n.en.ts`; `src/ui/i18n.locales/{es,es_ES,fr_FR,fr_CA,en_CA,it_IT,de_DE,zh_CN,zh_TW,ko_KR,ja_JP,pt_BR,ru_RU}.ts`; `src/ui/i18n.resolved.sha256` | `scripts/i18n_resolved_hash.mjs` (`i18n:hash`) | `tests/i18n_resolved_equivalence.test.ts` | Monolith split into en base + 13 nested `: typeof en` locale files + thin runtime; all public exports preserved; resolved table byte-identical. Locale files are created NESTED here (Phase 3 flattens them). `en`/locales are assembled by spreading shared content layers (`shellStrings`..`mergeExtra`, now exported from `i18n.en`) + `worldNames` (from `world_entity_i18n`) + a `gameStrings` variant + inline literals; the data is NOT one literal per locale. |
| 2 | `src/ui/i18n.resolved.generated.ts` (generated dense 14-locale table, committed + reproducibility-checked) | `scripts/i18n_build.mjs` (`i18n:build`; wired into `npm run build` before vite + `pretest`) | reproducibility check folded into `tests/i18n_resolved_equivalence.test.ts` (tracked-file + `git diff --exit-code` after regen); Phase 2 QA added `tests/i18n_build_gapfill.test.ts` (fill-from-English + preserve-present on the resolved table) | Generated nested table, each locale `: EnTranslations` (= `typeof en`), do-not-edit banner; overlays each locale onto a deep copy of `en` and fills missing leaves from English. Runtime `translations` + the four read-paths (`t`/`translationValue`/`hasTranslation`/`tOptional`) repointed at it - the direct-read gotcha below is now CLOSED. `EnTranslations` added to `i18n.en`. Byte-identical to Phase 1 (SHA `d9db528..`). Generator imports the Phase 1 SOURCE modules (`i18n.en` + `i18n.locales/*`), never `i18n.ts` or the generated file, so no circular import. |
| 3 | (no new src files; the 13 `src/ui/i18n.locales/<lang>.ts` were converted in place from nested `: typeof en` to flat `Record<string, string>`) | `scripts/i18n_flatten.mjs` (shared flatten/unflatten contract, used by the build); `scripts/i18n_flatten_locales.mjs` (one-time migration, not wired into anything) | `tests/i18n_flat_overlay_dense.test.ts` (TEMPORARY: exact key-set vs `flatten(en)` + non-empty strings per overlay; delete in Phase 6); `tests/i18n_overlay_key_membership.test.ts` (Phase 3 QA, PERMANENT: every overlay key is a member of `Leaves(en)` + positive teeth that a typo'd/invented/misspelled dotted key is rejected; subset guard survives the Phase 6 sparse relax) | Main-table locales are now STANDALONE flat dotted-key overlays (full-inline, no imports), keyed in `en`'s leaf order, 1925 dense keys each. Build unflattens each overlay onto a deep copy of nested `en` -> byte-identical artifact (SHA `d9db528..`, blob `90b4326..`, both unchanged). `en`/`i18n.en.ts` and runtime `i18n.ts` UNTOUCHED. **ISLANDS NOT FLATTENED (deferred to Phase 4):** `world_entity_i18n.ts` non-English data is now superseded by the inlined overlays + has zero runtime path-indexers (its `.en` slice still feeds nested `en`); `talent_i18n.ts` is a separate non-table channel with FUNCTION-valued `localeText` leaves that cannot be a flat string map. Overlay type is `Record<string, string>` not `Record<TranslationKey,string>` (Leaves depth 5 < deepest real leaf depth 6). `flatten`/`unflatten` throw on dotted segments / prefix collisions. |
| 4 | (none; `src/ui/world_entity_i18n.ts` reduced to English-only - dead non-English datasets + `makeLocaleWorldEntities` deleted) | `scripts/i18n_build.mjs` gained a data-driven `DIALECT_BASE` map (es_ES->es, fr_CA->fr_FR, en_CA->en) + base-then-dialect merge | `tests/i18n_flat_overlay_dense.test.ts` made dialect-aware (10 dense bases key-exact vs `en`; 3 dialect overlays subset + non-empty + every key diverges from base + strictly sparser); Phase 4 QA added `tests/i18n_dialect_resolution.test.ts` (resolved-table base-fallthrough + override; omitted dialect key resolves to base not en) | Dialect dedup: es_ES/fr_CA/en_CA overlays are now divergence-only (72/37/3 keys); build resolves a dialect as nested `en` -> base overlay -> dialect overlay, omitted keys falling through to base then English. Both `{} as` compiler bypasses gone: `world_entity_i18n` casts removed by deleting the dead non-English data (English-only now; only `.en` was ever consumed), `talent_i18n` `localeText` casts replaced by explicit aliases over a `satisfies`-typed base record. Resolved table byte-identical (SHA `d9db528..`, generated blob unchanged). `en` still NESTED; overlays still typed `Record<string,string>`. talent `titleOverrides` es_ES/fr_CA left as full blocks (live, not byte-gated, no cast; out of scope). |
| 5 | `src/ui/i18n.status.json` (generated registry); `scripts/i18n_hash.mjs` (shared content-hash helper); `scripts/i18n_blocked_seed.mjs` (canonical blocked-surface seed) | `scripts/i18n_scan.mjs` (`i18n:scan`; wired into `pretest` + `build` AFTER `i18n:build`) | `tests/i18n_status_registry.test.ts` (universe coverage + independent enHash re-derivation + pending-empty + blocked load-bearing + placeholder-in-hash sensitivity + regenerate/`git diff` reproducibility); `tests/localization_fixes.test.ts` `COPIED_ALLOW`/`ALLOW_V07_SLASH` swapped from literal Sets to registry views | No-LLM/no-network scanner. Key universe = en leaves (1925) U sim DICT (93) U server DICT (79) U admin DICT (181) = 2278 keys, scoped `<scope>:<key>`. Per key: `enHash` + per-locale `translated`(srcHash,by)/`pending`/`blocked`(reason); plus a `blockedSource` list for the v0.7 slash-command SOURCE strings (not keys). PURE function of source (no prior-output read) -> deterministic + reproducible. Cognates -> `blocked` rows (43); v0.7 slash -> `blockedSource` (105); `pending`=0 (still dense). srcHash recomputed from current English each run, so it equals enHash this phase (staleness DORMANT until Phase 6+). Resolved table byte-identical (SHA `d9db528..`). |
| 6 | (none new) | `scripts/i18n_build.mjs` emits a per-locale `pending` list (dialect-aware, mirrors `i18n_scan.mjs` `providedByLang`; empty while dense) | `tests/i18n_t_behavior.test.ts` (NEW: untracked throw/raw-key, pending English/hard-fail via injected key, index.html keys are real en leaves, empty-pending release-only); deleted `tests/i18n_flat_overlay_dense.test.ts` (temporary dense guard); S3 split into `s3_registered`(PR)/`s3_localized`(release) in `localization_fixes`; H3b + coverage quest/talent copied-English + registry empty-pending gated release-only; Phase 6 QA added the empty-pending GATE teeth test (shared `assertNoPending()` helper + a synthetic non-empty pending map that must FAIL, commit `79d0732`) | THE UNLOCK. Overlays now `Partial<Record<TranslationKey,string>>` (sparse legal); `TranslationKey`=`Leaves<typeof en,6>` (reaches depth-6 leaves; measured no tsc cost). `t()` throws on untracked in dev/test (raw key on release), renders English for `pending` on NON-RELEASE only, HARD-FAILS pending on release; `tOptional`/`hasTranslation`/`translationValue` unchanged (preserve `tEntity` fallback). Release flag `isReleaseBuild()`=`I18N_RELEASE=1` OR `import.meta.env.PROD`. CI split by ref: `pr-gate` (no tier flag) vs `release-gate` (`I18N_RELEASE_TIER=1`). Resolved-table byte-equivalence UNCHANGED (SHA `d9db528..`) - overlays stay dense, so `pending`=0 and the table is byte-identical; the `pending` English-fill is WIRED but DORMANT until Phase 7 depopulates. Baseline (`i18n.resolved.sha256`) NOT touched. |
| 7 | `scripts/i18n_glossary.json` (hand-maintained locked-terms glossary); `docs/i18n-scaling/worklist/` (GITIGNORED generated output dir) | `scripts/i18n_fill_worklist.mjs` (`i18n:worklist`; NOT wired into build/pretest - run manually before a release) | `tests/i18n_fill_worklist.test.ts` (14 cases: classification stopping rule, glossary `*`-expansion, sibling determinism; Phase 7 QA extracted a pure assembly core `buildWorklistOutputs`/`assertAutoFillableHasNoProse` and added 4 end-to-end tests - determinism + inputHash teeth, prose->humanRequired segregation, glossary/dialect resolution, the leak throw) | DATA-ONLY release-fill worklist. Reads `i18n.status.json` + `en`/overlays/DICTs + the glossary; emits per-language `{scope,key,english,placeholders,siblings}` for pending keys, splitting `autoFillable` (mechanical chrome) from `humanRequired` (prose, blocked-by-default). Glossary (brand verbatim + 9 classes + ability/zone/dungeon `*.name`) ships in every batch with each term's established per-locale form (own->base->en, mirrors the build). NO network/LLM/Date.now/Math.random; esbuild `data:`-URL load like `i18n_scan.mjs`. Deterministic + content-hash-cached no-op; staleness guard refuses a drifted registry. Resolution constants (`DIALECT_BASE`/`isPresent`/placeholder helper/`sourceModule`/DICT exports) byte-identical to scanner+build. Resolved table UNTOUCHED (`d9db528..`; tooling only, no depopulation). **BLOCKED-SURFACE LIST FINALIZED** (see Glossary section + prose classifier). OPEN: RFC 9.6 release-fill ownership / API key still unresolved (recorded in `docs/i18n-scaling/translation-workflow.md`). Reviews: privacy-security-review + cross-platform-sync + correctness all NON-BLOCKING. |
| 8 | `src/admin/i18n.en.ts` (flat authoritative admin English base, 181 keys -> 219 after the chat-filter additions); `src/admin/i18n.locales/{es,es_ES,fr_FR,fr_CA,en_CA,it_IT,de_DE,zh_CN,zh_TW,ko_KR,ja_JP,pt_BR,ru_RU}.ts` (flat per-locale overlays); `src/admin/i18n.resolved.generated.ts` (generated dense 14-locale admin table, committed + reproducibility-checked) | `scripts/i18n_admin_split.mjs` (ONE-TIME migration; refuses to re-run once `i18n.en.ts` exists, so it cannot un-sparse the overlays); `scripts/i18n_admin_build.mjs` (`i18n:admin`; wired into `pretest` + `build` BEFORE `i18n:scan`) | `tests/i18n_admin_catalog.test.ts` (two-tier gate via a synthetic injected pending key; bundle isolation = no `..` import out of `src/admin/`; overlay-key membership; admin.html `data-i18n` keys are real admin en keys; resolved-table reproducibility) | Admin catalog under the overlay + registry + release-gate model. DICT split into a flat en base + 13 flat overlays + a dense generated table (each locale `: AdminTranslations` = typeof en, dialect-aware es_ES->es/fr_CA->fr_FR/en_CA->en, English-fill for gaps, `pending` map). Runtime `src/admin/i18n.ts` reads the resolved table, re-exports a dense `DICT` (unchanged consumer surface for the H3/registry tests), and `t()` gains the game's pending/release/untracked policy (English-fill on non-release, hard-fail on a pending key at release, throw-on-untracked in dev / raw key on release; release flag `I18N_RELEASE=1` OR `import.meta.env.PROD`). Scanner admin scope moved from the dense `addDictScope` to a sparse, dialect-aware, blocked-seed-aware `addSparseScope` reading the en base + overlays, so an omitted admin key yields `pending`. 181 existing admin registry rows BYTE-IDENTICAL (0 state flips; 38 blocked cognates preserved); 38 NEW chat-filter/chat-moderation keys added English-only -> 456 `pending` rows (38 x 12 non-en/non-en_CA locales; en_CA inherits en). `i18n_fill_worklist.mjs` surfaces admin pending (autoFillable chrome) + now reads the admin SOURCE en (symmetry with the scanner). Hardcoded admin strings localized (chat-filter/chat-moderation panel in `main.ts`+`tables.ts` incl. the `main.ts:401` alert + a `title="Remove"` attr + input placeholders, and `admin.html`'s Chat Filter tab). Admin stays a SEPARATE bundle (~33 KB gzip; NO game locale table / `entities.*`/`abilityUi`/`classDetails` strings in the admin chunk). NO Postgres/DDL/persisted-state change. Game resolved table + `i18n.resolved.sha256` UNTOUCHED (`d9db528..`) - admin has its OWN resolved table with NO byte-equivalence baseline (reproducibility-gated instead). RFC 9.7 audit PASS (see gotchas). Reviews: privacy-security-review + cross-platform-sync + qa-checklist + correctness all NON-BLOCKING. |
| 9 | `scripts/i18n_pseudo.mjs` (shared deterministic accent+bracket transform); en_XA export added to `src/ui/i18n.resolved.generated.ts` + `src/admin/i18n.resolved.generated.ts` (generated, NOT in `translations`) | `scripts/i18n_build.mjs` + `scripts/i18n_admin_build.mjs` gained the en_XA emit (no new npm script) | `tests/i18n_pseudo_locale.test.ts` (12: generator parity/bracketing/placeholder-preservation/accent/determinism, exclusion from supportedLanguages/isSupportedLanguage/admin DICT, `?lang=en_XA` dev-on / release-off) | DEV-ONLY `en_XA` pseudo-locale. en_XA = `pseudoLocalize(resolved.en)` (fixed 1:1 ASCII->Latin accent map; brackets each leaf; preserves any `{...}` token EXACTLY; emoji/non-ASCII pass through), emitted as a SEPARATE export NOT in `translations` -> excluded by construction from supportedLanguages/picker/hreflang/release-gate/registry. Selectable via `?lang=en_XA` on non-release builds only; game `src/ui/i18n.ts` + admin `src/admin/i18n.ts` add a `pseudoActive` flag + `tableFor()` indirection gated by `!isReleaseBuild()` (base locale stays `"en"`, never persisted; the game `tableFor` keeps a `lang===currentLanguage` guard so explicit other-locale reads stay real - admin has no such read path). en_XA referenced ONLY behind a static `import.meta.env.PROD` guard -> Rollup tree-shakes the pseudo table out of prod (0 signature glyphs in both built bundles; gzip unchanged 1,121.05/33.17 KB). Game gate SHA UNTOUCHED (`d9db528..`; `i18n_resolved_hash.mjs` reads `supportedLanguages` only). Both generated tables regenerate deterministically + purely additive (+3496 game / +222 admin lines; the 14 locales/`translations`/`pending` byte-identical). Reviews privacy-security + cross-platform-sync + correctness all NON-BLOCKING. SURFACED LITERALS (deferred, fixing is out of scope): minimap side-button aria-labels `hud.ts:1386-1392`; Skin/Chroma swatch aria-labels `hud.ts:3688`/`3756` + `index.html:4653`/`4692`; chat tip `hud.ts:451`; version-footer "build" `main.ts:180` (full list + by-design/scope notes in progress.md Phase 9 section). |

## Glossary of locked terms (for the release fill; never auto-translate) - FINALIZED (Phase 7)
Project name "World of ClaudeCraft"; the 9 class names; ability names; zone/dungeon proper nouns. The release fill ships this glossary with every batch so per-locale terminology does not drift. Authored in Phase 7 as `scripts/i18n_glossary.json` (hand-maintained data file, edited like `i18n_blocked_seed.mjs`): `verbatim` = brand terms never translated; `categories` = en key-patterns (`classes.<9>`, `entities.abilities.*.name`, `entities.zones.*.name`, `entities.dungeons.*.name`) that `npm run i18n:worklist` expands and resolves per-locale. The BLOCKED-SURFACE (prose, human-required) is now FINALIZED as the worklist's blocked-by-default classifier: prose = `entities.*` + `classes.*` + `classDetails.lore.*` + `seo.*`; everything not on the mechanical-chrome allow-list also defaults to human-required; CJK talent names are out-of-registry (`talent_i18n.ts`), so structurally unreachable.

## Known gotchas / OPEN items
- CLOSED (Phase 2): `tOptional`, `hasTranslation`, and internal `translationValue` read the table directly - under sparse overlays a key can be genuinely absent at runtime. They are now pointed at the dense resolved artifact (the runtime `translations` is imported from `src/ui/i18n.resolved.generated.ts`), so all four read-paths read the dense table. Phase 3 onward must keep it that way - never repoint a read-path back at the raw `i18n.locales/*` overlays.
- BUNDLE (Phase 2, watch in later phases): the dense generated table fully inlines all 14 locales, so it loses the cross-locale reference-sharing the spread-assembled table had. Main client bundle gzip grew 966.77 -> 1120.64 KB (+15.9%). A naive repoint is far worse (+479.8 KB gzip): the `gameStrings` re-export from `i18n.en` drags the entire ~1 MB `i18n.en` base into the client bundle alongside the inlined table. Phase 2 sources `gameStrings` from the generated `en.game` instead, recovering ~326 KB gzip. The remaining ~154 KB is partly `world_entity_i18n` entity data being inlined in the table AND still bundled for the `hud`/`entity_i18n` runtime resolver - a candidate dedup if a later phase routes entity resolution through the table.
- The S3 guard is source-text scraping (regex over `sim.ts`/`hud.ts`); splitting it into `s3_registered`/`s3_localized` (Phase 6) preserves the scraping approach and its brittleness. Treat matcher coverage (including hud-local maps) as ongoing maintenance, not solved.
- GATE TOPOLOGY (clarified in Phase 2 QA, not a bug): the byte-equivalence hash and the reproducibility test cover DIFFERENT edges and are complementary - do not assume either alone catches all drift. Post-Phase-2 `scripts/i18n_resolved_hash.mjs` bundles `src/ui/i18n.ts`, whose per-locale exports come from the COMMITTED `i18n.resolved.generated.ts` - so the hash gates artifact -> baseline (a raw-SOURCE-only edit does NOT change the hash; proven in QA: mutating `es.ts` left the hash at `d9db528..`). `tests/i18n_resolved_equivalence.test.ts` regenerates then `git diff --exit-code`, gating source -> artifact (it caught the same `es.ts` mutation). A deliberate content change still trips the hash once regenerated+committed. Two readable per-key tests now also name the load-bearing semantics so a regression reports as an assertion, not an opaque hash mismatch: `tests/i18n_build_gapfill.test.ts` (fill-from-English / preserve-present) and `tests/i18n_dialect_resolution.test.ts` (omitted dialect key -> base, not en).
- RESOLVED (Phase 4): the main table and `world_entity_i18n` no longer have different dialect starting points. The main-table dialects are divergence-only overlays over a declared base; `world_entity_i18n` is English-only (its non-English data, including the es_ES/fr_CA aliases, was deleted). Dialect inheritance for BOTH the resolved table and (formerly) world_entity now flows through one mechanism: the build resolver's `DIALECT_BASE` merge.
- RESOLVED (Phase 4): the `{} as WorldEntityTranslations` cast (and the equivalent `{} as TalentLocaleText` in `talent_i18n.ts`) are gone. `world_entity_i18n.ts` is English-only (dead non-English data deleted), so there is no dialect cast there to bypass `: typeof en`; `talent_i18n.ts` `localeText` now assembles es_ES/fr_CA as explicit aliases over a `satisfies`-typed base record (no cast, no reassignment).
- RESOLVED (Phase 4) - PHASE 3 HANDOFF TO PHASE 4 (islands): `world_entity_i18n.ts` was reduced to English-only (the dead non-English slices + `makeLocaleWorldEntities` deleted; only `.en` was ever consumed, by `i18n.en.ts`). Maintainer chose DELETE over re-establishing it as the single entity source (which would have reversed Phase 3's full-inline). `talent_i18n.ts` stayed a separate function-valued channel read nested at runtime by `tTalent`; only its dialect casts were removed - its `titleOverrides` es_ES/fr_CA full blocks were left as-is (live, not byte-gated, no cast; deduping them was out of scope for this phase).
- RESOLVED (Phase 8): the admin DICT was brought under the overlay + registry + release-gate model. It is now a flat en base (`src/admin/i18n.en.ts`) + 13 sparse flat overlays (`src/admin/i18n.locales/*`) + a dense generated table (`src/admin/i18n.resolved.generated.ts`, built by `scripts/i18n_admin_build.mjs` / `npm run i18n:admin`). The 181 existing admin keys stayed fully translated (their registry rows are byte-identical; 38 blocked cognates preserved), so 14-locale completeness did NOT regress; the unlock is that NEW admin keys can ship English-only (pending) and are gated at release.
- RESOLVED (Phase 8) - FIXED: the hardcoded `window.alert('Choose a custom chat mute expiry.')` at `src/admin/main.ts:401` (and the rest of the previously-raw-English chat-filter/chat-moderation operator surface) is now localized through the admin `t()`. All 8 `window.alert` sites route through `t()`/`localizeAdminError`; the `admin.html` Chat Filter tab is `data-i18n="nav.chatFilter"`.
- PHASE 8 ADMIN PENDING IS LIVE (by design, not a bug): the 38 new chat-filter/chat-moderation keys were added English-only (the post-unlock contributor workflow: add to the en base, do NOT edit the overlays), so the registry now carries 456 admin `pending` rows. At the PR tier this is LEGAL (the dense table English-fills them); at the RELEASE tier `tests/i18n_status_registry.test.ts` (`pending===0`) and the admin H3b copied-English check FAIL until the maintainer fills them via `npm run i18n:worklist` (admin pending classify as autoFillable chrome). This is the two-tier gate working - NOT a completeness regression of the existing 181 keys. The maintainer fills these at release like any other pending slice.
- RFC 9.7 NON-CLIENT-CONSUMER AUDIT (Phase 8) - PASS: nothing outside the client seam can leak a `pending`-English admin string to a real user AT RELEASE. Checked: (1) `index.html` hreflang links are language-routing URLs (no translation) and its `data-i18n-content` meta (`seo.title`/`seo.description`) + `document.title` are GAME-side, pinned to real `en` leaves by `tests/i18n_t_behavior.test.ts`, with game `pending` empty at release - untouched by Phase 8. (2) `admin.html` static `<title>` + `data-i18n` defaults are overwritten at startup by `localizeStatic()` via the admin `t()`; every admin.html `data-i18n` key is a real admin en key (new test), so `localizeStatic` cannot throw-on-untracked, and the admin release gate requires `pending=0` so those keys are filled before any release. (3) The admin DICT / resolved table English-fills pending keys on non-release builds only; the admin `t()` hard-fails a pending key at release as a never-fires backstop. The `en_XA` pseudo-locale exclusion is deferred to Phase 9.
- A cooldown timer at ~`src/ui/hud.ts:1674` uses `Math.ceil(cd).toString()` instead of `formatNumber` (number-localization bypass). Note for Phase 6/8 polish; not load-bearing.
- `scripts/i18n_flatten_locales.mjs` is a ONE-SHOT migration: it already ran and now THROWS on re-run (the locale files are flat, and `flatten` rejects their dotted top-level keys by the fail-loud contract). Do NOT re-run it to onboard a new locale in a later phase - author the new overlay flat directly (copy an existing `src/ui/i18n.locales/<lang>.ts` shape) or unflatten/flatten by hand. Comments corrected in Phase 3 QA (commit `c5f6738`).
- PHASE 5 STALENESS IS DORMANT (by design, not a bug): the scanner recomputes `srcHash` from the CURRENT English each run and never reads its prior output (pure-from-source = maximally reproducible). So `srcHash` always equals the per-key `enHash` this phase, and the `srcHash !== enHash -> pending` staleness path never fires - `pending` is empty only because everything is still dense AND nothing is stale. To make staleness LIVE in Phase 6+, the fill pipeline must persist a translation-time `srcHash` that the scanner carries forward (or the scanner must read its prior output for present translations); decide that when sparseness lands. Do NOT add carry-forward casually - it trades the from-scratch==incremental reproducibility guarantee for live staleness.
- PHASE 5 REGISTRY UNIVERSE IS en-DRIVEN: the scanner enumerates DICT scopes by `Object.keys(DICT.en)`, so a key present in a non-`en` DICT locale but ABSENT from `DICT.en` would be silently dropped from the registry and the registry-in-sync test (which checks the en-derived universe) would not catch it. Currently there are 0 such keys, and DICT key parity across all 14 locales is independently enforced by `tests/server_i18n.test.ts` + `tests/localization_coverage.test.ts` (H3 `checkDict`), so it is covered - just not by the new registry test. Keep those parity tests; if a future DICT legitimately gains a non-en-only key, extend the scanner's universe.
- BYTE FIGURES, three of them, do not conflate: the byte-equivalence GATE is the canonical key-sorted serialization SHA `d9db528..` / 1,584,856 bytes (`i18n_resolved_hash.mjs`). The generated FILE `src/ui/i18n.resolved.generated.ts` is now 2,003,732 bytes on disk (Phase 6 added the `pending` export; was 2,003,477 in Phases 2-5) - its on-disk sha256/git-blob therefore changed, but the GATE SHA `d9db528..` did NOT (the `pending` export is not part of `translations`). Only `d9db528..` is the gate.
- PHASE 6 RELEASE FLAG (`isReleaseBuild()` in `src/ui/i18n.ts`): release = `process.env.I18N_RELEASE === '1'` (tests + the release build step) OR `import.meta.env.PROD === true` (the real Vite production build); dev server + vitest leave both false. Read LAZILY on the cold (miss/pending) path so the hot hit path is unchanged and a test can flip it per-case. CONSEQUENCE, documented in code: a NON-release build that still carries `pending` keys MUST NOT be deployed (it would render English for them). Two SEPARATE env vars, do not conflate: `I18N_RELEASE` flips `t()` runtime release semantics (per-test); `I18N_RELEASE_TIER` selects which TESTS run (the release CI job sets it; it does NOT set `I18N_RELEASE`).
- PHASE 6 BASELINE ROLE EVOLVED (important for Phase 7): through Phase 5 the byte-equivalence baseline (`src/ui/i18n.resolved.sha256`) was FROZEN (refactors preserve output). From Phase 6 the resolved table legitimately CHANGES with content, so the baseline becomes a tracked SNAPSHOT: a PR that changes the resolved table (adds/edits/depopulates a key) MUST regenerate the artifact AND `i18n.resolved.sha256` together (both committed), or `tests/i18n_resolved_equivalence.test.ts` red-fails. Phase 6 itself did NOT move it (`d9db528..`) because overlays stay dense; the gate proof confirmed that depopulating one key moves the hash to a new value that a real PR would commit.
- PHASE 6 FORWARD-CONSIDERATION (for Phase 7's actual depopulation): Slice C release-gated the quest/talent copied-English content checks + the empty-`pending` assertions + `s3_localized` (the phase's explicit scope), but OTHER specific-translation / copied-English assertions still run at the PR tier: `localization_coverage` item copied-English (`should provide every item translation`), the class-ability `yd`-abbrev check, the world non-quest spot-checks (de_DE/zh_CN/ko_KR/... `tEntity` equals), the zh_TW `dungeonInstanceBusy` assertion, and the `t()` nested-key sanity (`nav.home`=="Inicio"). These pass while overlays are dense, but a Phase 7 PR that depopulates one of the keys they pin would fail the PR gate. Phase 7 must either release-gate those assertions too, or only depopulate keys not pinned by a PR-tier test. (Phase 6 proved the gate with a sample key - `entities.quests.q_wolves.objectives.0.label` in es - chosen to be unpinned at the PR tier.)
