# progress.md - i18n Lazy Locales

Live status. Each phase session updates its own row + checklist in the SAME commit as its code.

## Status table
| Phase | Status | Started | Completed |
|-------|--------|---------|-----------|
| 1 - Per-locale emit split | COMPLETE | 2026-06-17 | 2026-06-17 |
| 1 QA | COMPLETE (WITH FOLLOWUPS) | 2026-06-18 | 2026-06-18 |
| 2 - Async loader + bootstrap | COMPLETE | 2026-06-18 | 2026-06-18 |
| 2 QA | COMPLETE | 2026-06-18 | 2026-06-18 |
| 3 - The lazy flip | COMPLETE | 2026-06-18 | 2026-06-18 |
| 3 QA | COMPLETE | 2026-06-18 | 2026-06-18 |
| 4 - Modulepreload + first-paint perf | COMPLETE | 2026-06-18 | 2026-06-18 |
| 4 QA | COMPLETE | 2026-06-18 | 2026-06-18 |
| 5 - Artifact / CI / determinism hygiene | COMPLETE | 2026-06-18 | 2026-06-18 |
| 5 QA | COMPLETE | 2026-06-18 | 2026-06-18 |
| 6 - i18n.en.ts directory split | NOT STARTED | | |
| 6 QA | NOT STARTED | | |

Status values: NOT STARTED / IN PROGRESS / COMPLETE / COMPLETE (WITH FOLLOWUPS) / BLOCKED.

## Phase 1 - Per-locale emit split (Doc Step 1)
Deliverables:
- [x] `scripts/i18n_build.mjs` emits `src/ui/i18n.resolved.generated/` (one dense `<lang>.ts` per locale with its `: EnTranslations` annotation, plus `en_XA.ts`) instead of the single file.
- [x] Generated `index.ts` barrel re-exports every locale + `en_XA` + `pending` + assembles the `translations` map (exact import surface preserved).
- [x] Generated `loaders.ts` exports `LOCALE_LOADERS` (dynamic-import thunk per non-en locale, NOT `en` / NOT `en_XA`) and `SUPPORTED_LANGUAGES` (all 14: en + 13, not en_XA).
- [x] Generated `pending.ts` exports `pending`.
- [x] `I18N_OUT_DIR` env override added; emit is atomic. **Deviation from the doc's `rmSync`+recreate:** uses per-file temp-write + `renameSync` + orphan-sweep instead. A bare `rmSync(dir)` makes every slice momentarily ABSENT, and a concurrent Vitest worker resolving `./en_XA` through the barrel (while the two reproducibility tests regenerate the dir) then fails with "Cannot find module". Temp+rename keeps every module path continuously present and atomically replaced, still leaves no orphan (the sweep deletes any stale `*.ts`), and is strictly crash-safer (no torn/empty dir). Verified: full `npm test` green.
- [x] `scripts/i18n_admin_build.mjs` mirrors the same directory transform into `src/admin/i18n.resolved.generated/` (parity only; admin stays static).
- [x] `scripts/i18n_scan.mjs` reads the new directory shape. **No edit needed:** the scanner reads the SPARSE SOURCE overlays (`i18n.en` + `i18n.locales/*` + the admin twin + sim/server DICTs), never the resolved table, so the dir split does not touch its inputs and `i18n:scan` produces a byte-identical `i18n.status.json` (verified clean `git diff`). The deliverable is satisfied by construction.
Acceptance:
- [x] `npm run i18n:build && npm run i18n:admin && npm run i18n:scan && git diff --exit-code` (regenerates identically; new dirs staged).
- [x] `npm run i18n:hash -- --check` OK (SHA `d74aeb6..` unchanged - exact: `d74aeb631f37f3d8a4374ff9940e450e062aa4062c821ab3349ae7ada28b2e4d`). NB: that was the Phase-1 SHA on the release/v0.9 baseline; the 2026-06-18 v0.10.0 merge (5e78a85) re-baselined the resolved table to `9606d9cf..` (now the value committed in `src/ui/i18n.resolved.sha256`).
- [x] `npx tsc --noEmit` + `npm test` green (1542 passed / 9 skipped / 153 files); `npm run build` bundle-neutral: HEAD single-file main gzip 1,194.58 kB vs this dir-split 1,194.62 kB (+40 bytes). **NB: the doc's "1.13 MB" is stale; the real pre-existing size on this branch is 1.19 MB, unchanged by this phase.** `en_XA` + `loaders.ts` both tree-shaken out (0 pseudo glyphs, 0 `LOCALE_LOADERS` in dist main+admin).

## Phase 2 - Async loader + bootstrap (Doc Step 2)
Deliverables:
- [x] `src/ui/i18n.ts`: `resident` map (seeded `{ en }` + the boot language synchronously), `inflight` map, `ensureLocaleLoaded(lang)` (idempotent, coalescing, English-instant, failure-soft, shape-tolerant read `mod.default ?? mod[lang]`), `isLocaleResident(lang)`, `reportLocaleLoadFailure`; `tableFor()` gains the English fallback. **DEVIATION (intentional):** `tableFor()` is `resident[lang] ?? translations[lang] ?? resident.en!`, NOT the packet's literal `resident[lang] ?? resident.en!`. The `translations[lang]` middle term is the still-static backstop that keeps Phase 2 byte-for-byte unchanged: ~6 test files (`homepage_foundation`, `sim_item_i18n`, `quartermaster_gear`, `chat_context_menu`, `server_i18n`, `localization_coverage`, `localization_fixes`) call `setLanguage(non-en)` then `t()` synchronously without an await, and the packet schedules their await-fixes for Phase 3 - so without the backstop they would regress to English and `npm test` would go red THIS phase. resident[lang] when loaded == translations[lang] content (same generated module), so it is provably byte-identical. **Phase 3 removes the `translations[lang]` term** when it drops the static barrel import; that is the natural site of the change.
- [x] `setLanguage` stays synchronous and unchanged in signature (does NOT load); `supportedLanguages` derives from `SUPPORTED_LANGUAGES` (`[...SUPPORTED_LANGUAGES]`, pinned equal to `Object.keys(translations)` by `i18n_emit_shape`).
- [x] `src/main.ts`: `await ensureLocaleLoaded(getLanguage())` after the loading-screen paint, before `mountGameUi` in `startGame` (behind the loading screen); `await ensureLocaleLoaded(selected)` in the (now async) picker handler before `setLanguage`; homepage shell (`wireStartScreens`) awaits before its initial `translatePage` (`.then(translatePage, translatePage)` so the English fallback still renders and no rejection escapes).
- [x] 3 new `en` keys: `settings.languageLoadFailed`, `settings.languageLoadUnavailable`, `settings.languageLoading` (TOP-LEVEL `settings` namespace, NOT `game.settings`; rendered via `t()` at the picker + a new `#lang-select-status` aria-live span in index.html).
- [x] Admin mirror: `ensureAdminLocaleLoaded` (+ `isAdminLocaleResident`) awaited before `localizeStatic()` in `src/admin/main.ts` (async surface only; admin keeps every locale static, so the load body is unreachable - parity scaffolding, no lazy flip).
- [x] Maintainer filled the 3 keys in the 10 base locales (es, fr_FR, it_IT, de_DE, zh_CN, zh_TW, ko_KR, ja_JP, pt_BR, ru_RU); es_ES/fr_CA inherit via DIALECT_BASE, en_CA stays English, en_XA pseudo-localized. Registry `pending=0` held; release-tier gate dry-run green.
Acceptance:
- [x] `npm test` green (224 files / 1962 passed / 9 skipped) + new `tests/i18n_lazy_loader.test.ts`: `t()` is synchronous and correct for a non-en `currentLanguage` before AND after an awaited `ensureLocaleLoaded` (pre-await via the static backstop, post-await via resident), plus English-instant, coalescing, and the 3 new keys.
- [x] `i18n:hash --check` OK at the re-baselined SHA `3254ea95..` (was `9606d9cf..`; the move is exactly the 3-key fill - diff is purely additive, only the 3 keys across all 15 slices); `tsc --noEmit` green. (`?lang=es` no-flash is structurally guaranteed by the await sitting behind the painted loading screen; full browser confirmation belongs to Phase 2 QA / Phase 3.)
- [x] Bundle ticked up slightly as expected (main gzip 1,264.62 kB vs the v0.10 Phase-1-QA baseline 1,263.10 kB, +1.52 kB: loader code + the 3 keys x locales in the still-static main chunk). NOT advertised as a win; the lazy win is Phase 3.

## Phase 3 - The lazy flip (Doc Step 3)
Deliverables:
- [x] `src/ui/i18n.ts` imports only `en` + `pending` + `LOCALE_LOADERS` + `SUPPORTED_LANGUAGES` (plus dev-only `en_XA` behind the PROD guard), each from the SPECIFIC generated module (`./i18n.resolved.generated/en` etc.), NOT the index barrel; the 13 non-en statics are no longer eagerly imported for use. `tableFor` drops the `translations[lang]` backstop -> `resident[lang] ?? resident.en!`; the boot pre-seed line is removed; `isSupportedLanguage`/`getStoredLanguage`/`SupportedLanguage` derive from `SUPPORTED_LANGUAGES`. `t()` stays synchronous.
- [x] Tree-shake probe: **Option 3a HELD.** Kept the pure `export { en, es, ... } from './i18n.resolved.generated'` re-export (dead in the app -> Rollup tree-shakes it). `npm run build`: `main-*.js` gzip **720.02 kB** (vite reporter; system `gzip -c | wc -c` = 714,715 B) - **down 544.6 kB from the pre-flip 1,264.62 kB**. No 3b needed.
- [x] Fix `tests/homepage_foundation.test.ts`: `await ensureLocaleLoaded(lang.code)` before the synchronous `t()` assertion (the Spanish test + the 12-locale loop).
- [x] Fix `tests/i18n_t_behavior.test.ts`: re-pointed the pending-injection mock to the new seam - mocks `./i18n.resolved.generated/es` (so `resident.es` carries the synthetic key after the await) AND `./i18n.resolved.generated/pending`, then `await ensureLocaleLoaded("es")`. Mock shape `{es, default}` is faithful to both the named-export source `.ts` and the production default chunk.
- [x] New tests: loader-rejection (spy `LOCALE_LOADERS.es` reject) -> rejects softly, t() falls back to English, no crash, retry succeeds; the BEFORE/AFTER test rewritten to prove English-fallback pre-await + localized-sync post-await; pending/release hard-fail preserved. Matrix files (`localization_coverage`, `localization_fixes`, `quartermaster_gear`, `chat_context_menu`) gained `beforeAll`/per-site `await ensureLocaleLoaded` to guarantee residency.
- [x] `src/main.ts`: wrapped the void-invoked `startGame` boot-await in try/catch (English fallback); `wireStartScreens` gates `#start-screen` with `visibility:hidden` (preserves layout) until the boot locale resolves, revealing after `translatePage()` on both resolve AND reject - only when the boot locale is not already resident (English/resident skip the gate).
Acceptance:
- [x] `dist/assets/`: `main-*.js` gzip 720.02 kB (down from 1,264.62 kB pre-flip - English-only main chunk); **13 content-hashed game locale chunks** (es..ru_RU incl. dialects es_ES/fr_CA/en_CA, ~40 KB gzip each, de_DE=41,223 B); `en` NOT a separate chunk.
- [x] Default-English load network trace (prod build via puppeteer): ZERO `es-*.js`..`ru_RU-*.js` requests; German `"Spielen"` NOT baked into `main-*.js`.
- [x] `i18n:hash --check` OK (SHA `3254ea95..` did NOT move); `npm test` green (224 files / 1964 passed / 9 skipped); `tsc --noEmit` green.
- [x] `?lang=es` + one CJK (`?lang=zh_CN`) render fully localized: chunk lazy-fetched, gate hid `#start-screen` during fetch, ZERO English-visible-while-gated samples (no flash), final nav.play = "Jugar" / "开始游戏". `visibility:hidden` preserves layout (no shift).

## Phase 4 - Modulepreload + first-paint perf (Doc Step 4 preload deliverable)
**DONE 2026-06-18. Both mechanisms shipped (locked decision 8). The data decisively proves the `<link>` is the only fix for the runtime-keyed waterfall.**
Deliverables:
- [x] Inline boot `<script>` in `index.html` `<head>` (right after `<link rel="manifest">`, before any module) reads `localStorage.getItem('locale')` and, for a stored non-en locale, injects ONE `<link rel="modulepreload" crossorigin="anonymous" href="...">` for that locale's content-hashed chunk before the main module parses. The hashed URL comes from a build-templated `{ locale: hashedChunkUrl }` map. `crossorigin='anonymous'` matches Vite's own bare-`crossorigin` module tags (both = the "Anonymous" CORS state) so the dynamic import dedupes to ONE fetch (verified: zero double-fetch). Hardened: `typeof href !== 'string'` guard (rejects a `__proto__`-style stored value) + the templated JSON escapes `<` (no `</script>` breakout).
- [x] Build hook: `build.manifest:true` in `vite.config.ts` emits `dist/.vite/manifest.json`; a `closeBundle` plugin (`i18nModulepreloadPlugin`) calls `templateModulepreload` (`scripts/i18n_modulepreload.mjs`, pure + unit-tested) to resolve each non-en locale's hashed chunk from the manifest and template the map over the `__I18N_LOCALE_CHUNKS__` sentinel in `dist/index.html`. Build-only: in dev the sentinel is undefined and the inline script's try/catch makes it a no-op. A missing chunk / missing sentinel is a hard build error (fail-closed).
- [x] Runtime prefetch helper `prefetchLocale(lang=currentLanguage)` in `src/ui/i18n.ts`, auto-fired on module eval in a browser (`if (typeof window !== "undefined") prefetchLocale()`), starts the current locale's import the moment i18n.ts evaluates (mechanism 1). No-op for English / resident / non-browser; swallows the rejection (the awaited bootstrap/picker still owns the failure UI). `t()` and `setLanguage` UNTOUCHED + synchronous.
- [x] No speculative preload: only the ONE stored/current locale; the other 12 are never referenced.
Acceptance:
- [x] Throttled (Slow-4G + 4x CPU, median of 5) prod-build network trace via `vite preview` (`scripts/i18n_modulepreload_trace.mjs`): **stored es = exactly 1 request, High priority, NO double-fetch, starts ~1261ms (concurrent with main at ~1452ms) and FINISHES ~2539ms - NO main-then-locale waterfall.** Contrast `?lang=es` (prefetch only, NO link, the faithful no-effective-preload baseline) = the es chunk starts ~21213ms (only AFTER main finishes ~20324ms + executes) and finishes ~22965ms (the waterfall). **The link cuts locale-ready ~23.0s -> ~2.5s; the prefetch alone cannot fix it (it lives inside main).**
- [x] Default-English visitor = **ZERO non-en chunk requests** (inline script injects nothing for an empty/absent stored locale; `prefetchLocale` returns early for `en`). The zero-non-en-bytes guarantee holds.
- [x] `npm run build` green; hook logged "templated 13 locale chunk URLs"; `dist/.vite/manifest.json` resolved the correct hashed filename per locale; sentinel fully replaced (0 left). Main-chunk gzip **720.04 kB** (Phase 3 win preserved; manifest is metadata). `INEFFECTIVE_DYNAMIC_IMPORT` warnings admin-only (game barrel still tree-shaken). en_XA pseudo TABLE tree-shaken (0 glyphs in main; the lone `en_XA` string is the pre-existing `DEV_PSEUDO_LOCALE` literal).
- [x] `npm run i18n:hash -- --check` OK, **SHA UNCHANGED at `3254ea95..`** (enabling the manifest did not perturb the bundle). `npx tsc --noEmit` clean; `npm test` **225 files / 1974 passed / 9 skipped** (+1 file/+11 cases: `tests/i18n_modulepreload.test.ts`). Generated dirs `git diff --exit-code` clean.
- [x] Mobile (390x844 @3x, Slow-4G+4xCPU, `scripts/i18n_modulepreload_mobile.mjs`): stored-es first paint **CLS 0.0729 (< 0.1 "good")**, fully localized (`lang=es`, "JUGAR" / "En Linea" / "WOC es nuestro token comunitario"), clean layout in `tmp/i18n_modulepreload_mobile_es.png`; english CLS 0. The non-zero es CLS is the homepage static-English->Spanish marketing-copy text-swap reflow (content OUTSIDE the `#start-screen` visibility gate) - a PRE-EXISTING Phase 3 behavior; Phase 4 adds zero rendered DOM and shrinks the English-visible window.
Review: `qa-checklist` = **PASS** (0 BLOCKING / 0 SHOULD-FIX; one coverage observation - the crossorigin single-fetch match is proven by the E2E double-fetch check, not a unit test, which is acceptable since browser dedup is not unit-testable). LIGHT `privacy-security-review` = **PASS** (0 BLOCKING / 0 SHOULD-FIX; 2 NICE-TO-HAVE defense-in-depth hardenings APPLIED: the `typeof href` guard + the `<` escape. No CSP exists to regress; the inline script is consistent with the pre-existing gtag inline script).

## Phase 5 - Artifact / CI / determinism hygiene (Doc Step 4 CI/git)
**DONE 2026-06-18. Pure hygiene: zero runtime change, zero bundle change, resolved-table SHA UNCHANGED at `3254ea95..`.**
Deliverables:
- [x] `git rm --cached src/ui/i18n.status.json` (~4.75 MB build/test-only) + gitignored it (`.gitignore`). The working-tree file stays on disk and is regenerated by `pretest` / the new CI `i18n:gen` step; content readers (`i18n_status_registry`, `localization_fixes`, `i18n_fill_worklist`) are unaffected - only the committed-bytes `git diff` reproducibility check broke, and that was swapped for determinism.
- [x] `.gitattributes` (NEW - the file did not exist): marks `src/ui/i18n.resolved.generated/**` AND `src/admin/i18n.resolved.generated/**` `linguist-generated` (dirs STAY committed; display hygiene only).
- [x] `package.json`: added `i18n:gen` = `npm run i18n:build && npm run i18n:admin && npm run i18n:scan`. `pretest`/`build` left UNCHANGED (they already inline the same three).
- [x] `.github/workflows/ci.yml`: added a `Generate i18n artifacts` (`npm run i18n:gen`) step to BOTH `pr-gate` and `release-gate`, after `npm ci`, before the test step. The two-tier gate structure (ref-split `if:`, `I18N_RELEASE_TIER: '1'` on release) is byte-for-byte untouched; the step is identical in both jobs (generators do NOT branch on the tier flag).
- [x] `tests/helpers/i18n_determinism.ts` (NEW): `assertDeterministic({ script, outFiles?, env?, cwd? })` double-generates into two throwaway temp dirs via `I18N_OUT_DIR`, PERTURBING `TZ`/`LC_ALL`/`LANG` + the temp-dir path between runs, pins the Node binary to `process.execPath`, and asserts byte-identity (throws on a diff / missing file / differing file set). `outFiles` omitted => compares the whole emitted tree. Mutation-proven non-vacuous (a TZ-dependent probe generator makes it throw; a constant generator passes). Pre-req: added the `I18N_OUT_DIR` override to `scripts/i18n_scan.mjs` (it previously hardcoded `OUT_PATH`; only the two build scripts honored it).
- [x] Replaced the committed-bytes reproducibility sub-suite in `tests/i18n_status_registry.test.ts` (the `git ls-files --error-unmatch` + `git diff --exit-code` on `status.json`) with an `assertDeterministic` double-generation check; KEPT every validation sub-suite (universe coverage, enHash re-derivation, counts consistency, blocked-row load-bearing, hash sensitivity, the release-tier `it.runIf(RELEASE_TIER)` pending===0 gate). `tests/i18n_resolved_equivalence.test.ts` KEPT its `git diff` freshness check on the committed resolved dir AND gained an `assertDeterministic` check.
- [x] Committed `src/ui/i18n.status.summary.json` (NEW, ~1.7 KB): `universeHash` (sha256 over `composite + enHash + placeholders` per key, emit order) + `counts` + per-locale `{translated,pending,blocked}` rollup + `scopes`/`locales`. NO per-key bodies, NO timestamps (a pure function of source -> regenerates byte-identically). Emitted by `scripts/i18n_scan.mjs` alongside the full registry; the registry test cross-checks it (counts equal, rollup tallies to totals and to keys-per-locale, universeHash re-derives independently) AND keeps a `git diff` freshness check on it.
Acceptance:
- [x] Fresh-clone sim: removed `src/ui/i18n.status.json` (absent, as on a fresh clone) -> `npm test` green (`pretest` regenerated it): **225 files / 1986 passed / 9 skipped** (PR tier; +4 vs Phase 4's 1982 = the net new determinism + summary cross-check cases).
- [x] `I18N_RELEASE_TIER=1 npm test` green on the translated tree (**225 files / 1994 passed / 1 skipped**); RED on a synthetic pending row (injected one pending row into the regenerable `status.json` -> the release-tier `pending===0` assertion failed; restored clean).
- [x] `git status` clean after `i18n:gen`; no megabyte file tracked (`status.json` gone from the index; only the ~1.7 KB summary is committed); `npm run i18n:hash -- --check` OK, **SHA UNCHANGED `3254ea95..`**; `npx tsc --noEmit` clean.
- [x] Review: `privacy-security-review` = **PASS** (0 BLOCKING / 0 SHOULD-FIX; CI step is a static `run:` with no untrusted interpolation, two-tier gate untouched, helper uses array-arg `execFileSync` with a non-attacker-controlled path, summary carries no secrets). `qa-checklist` = **PASS** (0 BLOCKING / 0 SHOULD-FIX; all 10 acceptance criteria re-derived). 3 NICE-TO-HAVE doc/DX items APPLIED this pass (the `i18n_fill_worklist.mjs` "committed" comment corrected to "generated/gitignored"; a friendly `npm run i18n:gen` existence-guard added to the two test files that bare-read the now-absent-able `status.json`).

QA (2026-06-18, Opus 4.8, max effort): **PASS-WITH-FOLLOWUPS (all follow-ups APPLIED).** Independent re-verification of all four STOP rules - fresh-clone green with `status.json` absent (isolated worktree: PR-tier 1986 pass), release gate **teeth** (synthetic pending row -> RED on `pending===0`, GREEN at PR tier), determinism gate **bite** (forced TZ-dependent emit -> RED, GREEN after revert), SHA unmoved `3254ea95..` (before/after regen + after build) - plus CI step-order/posture, `.gitattributes`, summary cross-check, dead-code (zero orphaned committed-bytes check on `status.json`), and `git status` clean post-build. Four review agents: `privacy-security-review` PASS, `qa-checklist` PASS, dead-code PASS, test-coverage **PASS-WITH-FOLLOWUPS** (1 SHOULD-FIX, surfaced an asymmetry). **Follow-ups applied (commits `b83af46` test, `9f3414d` ci; separate from this doc):** (1) SHOULD-FIX - extended the `assertDeterministic` perturbed-env gate to the admin resolved generator (`scripts/i18n_admin_build.mjs`); the game build + scanner were gated but the admin twin had only the committed-dir `git diff` freshness check (it passes -> admin generator is genuinely deterministic; latent gap, not a live bug). (2) NICE-TO-HAVE - committed non-vacuity proof for the release-tier `pending===0` gate (synthetic pending row trips both measured quantities; runs at both tiers). (3) NICE-TO-HAVE - pinned `process.execPath` (not bare `node`) in the generator-invoking freshness checks, matching the determinism helper. (4) NICE-TO-HAVE - CI `Verify committed i18n artifacts are fresh` fast-fail step (`git diff --exit-code` on the committed dirs + summary, both jobs; validated: passes clean, catches drift, excludes gitignored `status.json`). Post-fix gates: PR-tier **1988 pass**, release-tier **1996 pass** (+2 each), tsc clean, build green (main gzip **720.04 kB**, unchanged), SHA `3254ea95..`. **Deferred (none blocking):** (a) OS-level perturbation effectiveness in `assertDeterministic` is unasserted - if a CI host lacks `Asia/Kolkata`/`en_US.UTF-8` tzdata the TZ/LC_ALL axis could no-op; mitigated by the unconditional temp-path axis; not worth fragile env-probing. (b) Pinning `actions/checkout@v6` / `setup-node@v6` to commit SHAs (pre-existing, repo-wide supply-chain policy, out of this packet's scope - recommend a separate change; not guessing a SHA blind). **NEXT: run `docs/i18n-scaling/lazy-locales/phase-06-en-split.md` (Phase 6 - i18n.en.ts directory split).** PR target `release/v0.10.0`; branch NOT pushed - confirm with the maintainer before pushing.

## Phase 6 - i18n.en.ts directory split (Doc Q6 / Section 4.4.3)
Deliverables:
- [ ] Split `src/ui/i18n.en.ts` into `src/ui/i18n.en/` (`shell.ts`, `hud.ts`, `abilities.ts`, `quests.ts`, `items.ts`, `game.ts`, `_merge.ts`) + barrel `index.ts`; `i18n.en.ts` becomes a thin re-export (public surface unchanged).
- [ ] Each module keeps its exact content; no value changes (this is a pure module reorg).
Acceptance:
- [ ] Resolved table byte-identical -> the resolved-table SHA must not move during the phase: `npm run i18n:hash -- --check` stays green against the baseline committed in `src/ui/i18n.resolved.sha256` at the start of the phase (currently `9606d9cf..` after the 2026-06-18 v0.10.0 merge, NOT the `d74aeb6..` recorded under Phase 1, which was the old release/v0.9 baseline); `git diff --exit-code` on the regenerated dirs. A move within the phase is a real bug (pure module reorg), never a re-baseline.
- [ ] `npx tsc --noEmit` + `npm test` + `npm run build` green; public import surface from `i18n.en` unchanged.

## Notes (filled after completion)
- 2026-06-18: merged release/v0.10.0 (merge 5e78a85); resolved-table baseline re-generated to `9606d9cf..` (was `d74aeb6..`); Phase 1 emit-split intact, Phases 2-6 still pending.
- Phase 1: DONE 2026-06-17. The single `src/ui/i18n.resolved.generated.ts` (and admin twin) are now generated DIRECTORIES: `en.ts`..`ru_RU.ts` (14 dense `: EnTranslations` / `: AdminTranslations` slices) + `en_XA.ts` + `pending.ts` + `loaders.ts` + `index.ts` barrel. Directory-index import (`'./i18n.resolved.generated'` -> `index.ts`) resolves cleanly under moduleResolution "Bundler" (precedent: `src/render/characters/`); `src/ui/i18n.ts` + `src/admin/i18n.ts` needed ZERO change. SHA invariant because `scripts/i18n_resolved_hash.mjs` bundles `i18n.ts` EXPORTS, not file bytes. Two reality nuances vs the doc (see deliverables above): scanner needed no edit (reads source, not the resolved table); atomic emit uses temp+rename+sweep, not `rmSync` (the `rmSync` window broke concurrent barrel resolution in `npm test`). Tests touched: `tests/i18n_resolved_equivalence.test.ts` + `tests/i18n_admin_catalog.test.ts` repointed their reproducibility git-checks at the directory; the admin bundle-isolation check changed from a crude `startsWith("..")` to resolve-and-check-escape-from-`src/admin/` (the new in-dir `../i18n.en` type import is legitimate; a `../ui/...` game-table import is still caught). Nothing is lazy yet; all 14 locales still pulled through the static barrel. In-phase qa-checklist review run at completion (STEP 3); the dedicated Phase 1 QA session (`phase-01-qa.md`) is still NOT STARTED.
- Phase 1 QA: COMPLETE (WITH FOLLOWUPS) 2026-06-18. Verdict PASS-WITH-FOLLOWUPS. Validation matrix all green: byte-identity regen (`i18n:build`+`admin`+`scan` -> `git diff --exit-code` clean), `i18n:hash --check` OK at `9606d9cf..` (SHA did NOT move - layout-only change), `tsc --noEmit` exit 0, `npm test` 223 files / 1958 passed / 9 skipped, `npm run build` bundle-neutral (main gzip 1,263.10 kB; v0.10 re-measure of the stale 1.19 MB figure - all 14 locales still in the main chunk via the static barrel, as designed; the lazy win is Phase 3). 4 review agents (correctness / test-coverage / dead-code / qa-checklist) found 0 BLOCKING. Fixes applied this pass (all NICE-TO-HAVE + the 2 SHOULD-FIX addressed; commits separate from this doc):
  - **NEW test** `tests/i18n_emit_shape.test.ts` (the biggest gap): pins the emit SURFACE for game+admin - `translations` key set (14, en_XA excluded), `LOCALE_LOADERS` keys (13, no en/en_XA), `SUPPORTED_LANGUAGES` == `Object.keys(translations)` == runtime `supportedLanguages`, `pending` key set, and each loader thunk lazily resolves its slice. Plus a determinism + orphan-sweep block that drives both build scripts through the **`I18N_OUT_DIR`** override into an `os.tmpdir()` scratch dir (override branch was previously dead-untested), asserts byte-identical re-emit and that a planted `orphan_zz.ts` is swept - this makes the build-script comment ("used by the determinism test") truthful.
  - **Orphan-sweep hardened**: `writeModuleDir` in both build scripts now also prunes stale `*.ts.tmp` (a crash between `writeFileSync` and `renameSync` left one that the old `.ts`-only sweep ignored); `.gitignore` also guards `src/{ui,admin}/i18n.resolved.generated/*.tmp` so a crash leftover can never be committed. Output byte-identical (SHA unchanged).
  - **Stale-comment sweep**: refreshed the now-misleading single-file `i18n.resolved.generated.ts` references (the artifact is a directory) in `src/ui/i18n.ts`, `src/admin/i18n.ts`, `src/ui/i18n.en.ts`, `src/admin/i18n.en.ts`, `src/ui/CLAUDE.md`, `scripts/i18n_scan.mjs`, `scripts/i18n_admin_split.mjs`, `tests/i18n_build_gapfill.test.ts`, `tests/i18n_dialect_resolution.test.ts`.
  - **Admin-no-SHA asymmetry documented** (assessed NOT a gap - pre-existing): added a note at `scripts/i18n_resolved_hash.mjs` `BASELINE_PATH` that the admin table is intentionally gated by the `i18n_admin_catalog` reproducibility test, not a SHA baseline, so a later phase does not "restore" a file that never existed.
  - **Dead scratch removed**: `git rm fix_braces.mjs` (repo-root one-shot regex hack that mutated `src/sim/sim.ts`; no callers, not wired into npm, entered via merge-fixup `971fbd16` - out of the Phase 1 range but genuinely dead/hazardous).
  - Deferred (correctly out of scope): the `loaders.ts`/`SUPPORTED_LANGUAGES` "could drift later" note is now neutralized by the surface test asserting the two agree; a generalized `assertDeterministic` helper is already Phase 5's deliverable (this QA's scratch-dir determinism test is a narrower early instance - Phase 5 can fold it in).
- Phase 2: DONE 2026-06-18. Landed the async loader seam additively; nothing flipped to lazy. New symbols in `src/ui/i18n.ts`: `resident`/`inflight` maps, `ensureLocaleLoaded`, `isLocaleResident`, `reportLocaleLoadFailure`; `tableFor` English fallback (with the static backstop, see the deliverable note); `supportedLanguages` now from `SUPPORTED_LANGUAGES`. Awaited at three boundaries (startGame, picker, homepage shell) + admin mirror (`ensureAdminLocaleLoaded`). 3 new top-level `settings.*` keys filled across all locales (pending stayed 0). SHA re-baselined `9606d9cf..` -> `3254ea95..` (exactly the 3-key fill). Two review agents (qa-checklist COVERAGE + a correctness diff review) returned 0 BLOCKING / 0 SHOULD-FIX (verdicts PASS and SHIP); both independently confirmed the `tableFor` backstop deviation is correct and byte-for-byte preserving, the SHA move is only the 3 keys, and `t()` stays synchronous. The in-phase `.finally(() => translatePage())` on the homepage shell was changed to `.then(translatePage, translatePage)` per a QA nice-to-have (consume rejection so no unhandled rejection escapes once Phase 3 makes it a real fetch). **Phase 3 handoffs (not Phase 2 defects):** (a) drop the `translations[lang]` term in `tableFor` + the static barrel import when the lazy flip lands; (b) add a loader-rejection test (simulable only after the flip makes a 404 possible) -> English fallback, retry possible, picker shows `languageLoadFailed`; (c) the `startGame` bootstrap `await ensureLocaleLoaded(getLanguage())` rethrows on a real fetch reject (Phase 2 never rejects because the boot language is pre-seeded resident) - Phase 3 must wrap it so a failed first load still falls back to English. **Phase 2 QA is now COMPLETE (see the Phase 2 QA note below); NEXT: run `docs/i18n-scaling/lazy-locales/phase-03-lazy-flip.md` (Phase 3 - the lazy flip).**
- Phase 2 QA: COMPLETE 2026-06-18. Verdict **PASS** (0 BLOCKING, 1 SHOULD-FIX fixed, rest are Phase 3 handoffs). Deterministic matrix all green: `tsc --noEmit` exit 0; full `npm test` 224 files / 1963 passed / 9 skipped (was 1962 - the +1 is the strengthened loader test below); runtime row + `i18n_emit_shape` 6 files / 53 passed; `i18n:hash --check` OK at `3254ea95..` (the committed baseline - SHA did NOT move during QA); `I18N_RELEASE_TIER=1` registry gate 14/14 with **pending=0** (verified across 33,592 rows incl. admin); `i18n:build`+`i18n:admin`+`i18n:scan` regen byte-identical (`git diff --exit-code` clean - no hand-edits); `npm run build` green (main chunk 4.16 MB raw / ~1.255 MB gzip by direct `gzip -c`; the expected slight tick-up, NOT a win). Resolved slices verified +5/-0 additive across all 15 (the 3-key fill only); dialect inheritance correct (en_CA=English, es_ES inherits es, fr_CA inherits fr_FR, en_XA pseudo); the new top-level `en.settings` is a single non-duplicate literal (distinct from `gameStrings.settings` -> `game.settings.*`), no spread collision. 4 review agents (correctness PASS-WITH-FOLLOWUPS, qa-checklist PASS, test-coverage PASS-WITH-FOLLOWUPS, dead-code PASS) returned 0 BLOCKING / 0 SHOULD-FIX except the one test gap below.
  - **SHOULD-FIX applied** (test-coverage): the "coalesces concurrent loads" test did not actually prove coalescing - deleting the `inflight` short-circuit would still pass it. Strengthened in `tests/i18n_lazy_loader.test.ts` to prove the property via a loader-call-count spy (`vi.spyOn(LOCALE_LOADERS,'fr_FR')` -> `toHaveBeenCalledTimes(1)`; spy-through so fr_FR still becomes resident), with a `isLocaleResident('fr_FR')===false` precondition. NB: the agent's first suggested fix `expect(p2).toBe(p1)` is UNSOUND - `ensureLocaleLoaded` is `async`, so each call returns a fresh wrapper promise and outer-promise identity can never hold (it failed when tried). Also added a load-vs-select decoupling test (loading ja_JP while on en leaves `t()` English). Committed separately from this doc.
  - **Phase 3 handoffs surfaced by QA** (NOT Phase 2 defects - all no-ops while everything is static): (a) `tableFor` (`src/ui/i18n.ts:262`) `translations[lang]` middle term + the boot pre-seed (`:143`) must be dropped together with the static barrel import when the flip lands; (b) `startGame` boot-await (`src/main.ts:547`) has no try/catch and `startGame` is invoked as `void startGame(...)` with no `.catch`, so a Phase-3 real boot-locale fetch reject would be an UNHANDLED rejection (the picker and homepage shell already handle reject) - wrap it (English fallback + continue) in Phase 3; (c) the homepage shell await (`src/main.ts:2721`) is NOT behind any loading screen, so a Phase-3 stored non-en locale could flash English on the homepage before the fetch resolves - gate the localized homepage regions until the boot locale resolves; (d) add a loader-rejection (simulated 404) test once the flip makes a thunk failable - English fallback, no crash, retry possible, picker shows `settings.languageLoadFailed`.
- Phase 3: DONE 2026-06-18. The lazy flip. **Option 3a HELD - no 3b commit.** `src/ui/i18n.ts` static-imports only English eagerly (`en`/`en_XA`(dev)/`pending`/`LOCALE_LOADERS`/`SUPPORTED_LANGUAGES`, each from the SPECIFIC generated module, NOT the index barrel); the 13 non-en slices load lazily via `LOCALE_LOADERS[lang]()` into `resident`; `tableFor` is now `resident[lang] ?? resident.en!` (the `translations[lang]` backstop + the boot pre-seed were dropped together with the static barrel import). A pure `export { en, es, ... } from './i18n.resolved.generated'` re-export is retained for const-importers (the S3 guard `tests/localization_fixes.test.ts` + `scripts/i18n_resolved_hash.mjs`), which stayed green UNTOUCHED on imports - confirming 3a. **Tree-shake probe: main-chunk gzip 1,264.62 kB -> 720.02 kB (-544.6 kB / ~43%)**; 13 content-hashed game locale chunks (es..ru_RU incl. dialects es_ES/fr_CA/en_CA, ~40 KB gzip each); `en` not a separate chunk; prod-build puppeteer trace shows ZERO non-en chunk requests for an English visitor + `"Spielen"` absent from main. The `INEFFECTIVE_DYNAMIC_IMPORT` build warnings are ONLY for `src/admin/` (static by design, locked decision 4), never `src/ui/` - the signal the GAME barrel was tree-shaken. SHA UNCHANGED at `3254ea95..`. The breakage was BROADER than the doc's 2 named canaries (the v0.10 merge added many locale tests): 7 test files failed when the backstop was removed - fixed homepage_foundation (per-loop await, Canary 1), i18n_t_behavior (repointed the pending-injection mock from the dead barrel `translations` to the `./es` slice + `./pending`, Canary 2), i18n_lazy_loader (rewrote the Phase-2 pre-await assertion to English-fallback + added a loader-rejection test), and the matrix files localization_coverage/localization_fixes/quartermaster_gear/chat_context_menu (beforeAll/per-site `await ensureLocaleLoaded`). main.ts: boot-await wrapped (handoff a) + homepage `#start-screen` visibility:hidden gate revealed in a `finally` (handoff b + qa SHOULD-FIX #1). New tests: loader-rejection -> English fallback/no-crash/retry; sync-translated-after-await; pending hard-fail preserved. **In-phase review:** correctness diff = SHIP; qa-checklist = PASS (0 BLOCKING; SHOULD-FIX #1 fixed via the finally, SHOULD-FIX #2 [committed gate unit test] deferred to Phase 3 QA - main.ts is not cleanly unit-testable without an out-of-scope refactor; the E2E + the finally + the dual reject/resolve callback cover the gate). **NEXT: run `docs/i18n-scaling/lazy-locales/phase-03-qa.md` (Phase 3 QA).**
  - **Phase 3 QA handoffs:** (a) add a committed gate test (extract a small testable helper from `wireStartScreens`, or a jsdom test asserting `#start-screen` visibility is restored after BOTH a resolved and a rejected `ensureLocaleLoaded`, and that the gate only applies when the boot locale is non-resident) - qa SHOULD-FIX #2; (b) consider a non-vacuous floor on `localization_coverage`'s `missingEntityTranslationsForGroups(...) === []` assertions so a future removal of the all-locale `beforeAll` preload cannot pass silently against the English fallback (NICE-TO-HAVE - the check reads `resident.en` for a non-resident locale); (c) NICE-TO-HAVE: on a FAILED non-en boot fetch, `revealLocalized`/`translatePage` still sets `documentElement.lang` to the non-en tag while rendering English text (transient, self-corrects on a picker retry); set the lang attr from the actually-resident locale if desired; (d) NICE-TO-HAVE: no `aria-busy` during the homepage hold (Phase 4's modulepreload shrinks the hold toward zero).
- Phase 3 QA: COMPLETE 2026-06-18. Verdict **PASS** (0 BLOCKING, 0 SHOULD-FIX). The lazy flip delivered its payload win and held every invariant. **Hard gates re-verified directly by the QA lead:** `npm run build` green; `gzip -c dist/assets/main-*.js | wc -c` = **714,723 B** (vite reporter 720.02 kB; down from the pre-flip 1,264.62 kB - the ~544 KB drop is real); **13 content-hashed game locale chunks** (es 40,202 / de_DE 41,223 / ja_JP 43,527 / ru_RU 48,469 B gzip, dialects es_ES/fr_CA/en_CA present), `en` NOT a chunk; `INEFFECTIVE_DYNAMIC_IMPORT` warnings ONLY for `src/admin/`, never `src/ui/` (the tree-shake signal); main-chunk grep = ZERO main-TABLE non-en strings (Spielen/Jugar/开始游戏/Играть all 0) and en_XA pseudo fully tree-shaken from ALL dist chunks. `npm run i18n:hash -- --check` OK, **SHA UNCHANGED at `3254ea95..`** (bytes=1842416 - the flip moved no translation bytes). `npx tsc --noEmit` clean; `npm test` 224 files / 1964 passed / 9 skipped; `I18N_RELEASE_TIER=1` registry+i18n gate green (pending=0). **Network + no-flash trace (a fresh puppeteer run on the PROD build via `vite preview`, throttled Slow-4G + 4x CPU):** default-English visitor -> **ZERO non-en chunk requests**, `#start-screen` never gated; `?lang=es` -> requests EXACTLY `es-*.js`, gate applied, **0 post-gate English-flash samples**, final nav.play="Jugar"; `?lang=zh_CN` -> EXACTLY `zh_CN-*.js`, gate applied, 0 flash, final "开始游戏". `t()`/`setLanguage` confirmed synchronous; `ensureLocaleLoaded` `.catch` clears `inflight` (retry works) + re-throws; the `visibility:hidden` gate preserves layout (no shift) and is revealed in a `try/finally` so a `translatePage` throw cannot strand the homepage hidden. **Audit:** an adversarial multi-agent Workflow (correctness / test-coverage / dead-code review, each finding independently re-verified by a skeptic) + the `qa-checklist` agent. Verdicts: correctness PASS, dead-code PASS, qa-checklist PASS, test-coverage PASS-WITH-FOLLOWUPS. The single SHOULD-FIX (localization_coverage completeness assertions vacuous vs the English fill) was verified **INVALID** - it is a PRE-EXISTING structural property (the dense table is `: EnTranslations` so it has always English-filled; pre-flip `tableFor` used the same dense `translations[lang]`), already recorded as a NICE-TO-HAVE deferral, NOT a Phase-3 regression; the Phase-3-relevant residency regression IS still caught by the `not.toBe(entry.source)` checks. **One NICE-TO-HAVE applied** (commit `d64a819`, separate from this doc): `tests/i18n_lazy_loader.test.ts` rejection test gained an explicit `es.nav.play !== en.nav.play` floor so its English-fallback assertion cannot pass vacuously (mirrors the before/after test's existing floor).
  - **NICE-TO-HAVE followups (deferred, none blocking):** (a) the recorded gate-unit-test for `wireStartScreens` (qa SHOULD-FIX #2; main.ts not cleanly unit-testable without an out-of-scope refactor - the E2E + the finally + the dual reject/resolve callback cover it). (b) **The classAbility name-field floor canNOT be added as a blanket `not.toBe(entry.source)` like the item loop** - QA tried it and `es.entities.abilities.garrote.name === "Garrote"` (a Spanish loanword/cognate identical to English) fails it; a correct floor needs a cognate allow-list (scope creep), so the per-entry name coverage stays defended by the all-locale `beforeAll` preload only. (c) `src/main.ts` picker-handler comment (~:3471) and the `src/ui/i18n.ts:19-21` re-export-consumer comment are now slightly stale/under-enumerated (the picker can paint the loading status post-flip; the re-export also serves `localization_coverage`/`i18n_lazy_loader`/`i18n_status_registry`) - cosmetic doc-drift, left untouched to avoid churning a high-traffic file a concurrent session may hold. (d) the pre-existing unused `gameStrings` export, the failed-boot `documentElement.lang` transient, and `aria-busy` during the homepage hold (Phase 4 shrinks the hold). **NEXT: run `docs/i18n-scaling/lazy-locales/phase-04-modulepreload.md` (Phase 4 - modulepreload + first-paint perf).** PR target `release/v0.10.0`; branch NOT pushed - confirm with the maintainer before pushing.
- Phase 4 QA: COMPLETE 2026-06-18. Verdict **PASS-WITH-FOLLOWUPS** (0 BLOCKING / 0 SHOULD-FIX; 15 NICE-TO-HAVE confirmed, 4 refuted INVALID). Audit = a 5-dimension adversarial Workflow (correctness / test-coverage / dead-code, each finding re-verified by a skeptic) + the `qa-checklist` agent + a LIGHT `privacy-security-review`. Dimension verdicts: correctness PASS, test-coverage PASS-WITH-FOLLOWUPS, dead-code PASS, qa-checklist PASS, privacy-security PASS. **Hard gates re-verified directly by the QA lead:** `npm run build` green (main gzip **720.04 kB**, hook logged "templated 13 locale chunk URLs"); 13 content-hashed game locale chunks in `dist/assets`, **no `en-*.js` chunk**; `dist/.vite/manifest.json` emitted and the templated `{locale:url}` map in `dist/index.html` resolves **13/13** to real files (spot-checked es/de_DE/ru_RU hashes); the `__I18N_LOCALE_CHUNKS__` sentinel is fully replaced (0 left); `npm run i18n:hash -- --check` OK **SHA UNCHANGED `3254ea95..`** (bytes=1842416 - enabling `build.manifest:true` did not perturb the bundle); en_XA pseudo TABLE tree-shaken (only the 1 `DEV_PSEUDO_LOCALE` string literal in main, 0 pseudo glyphs); the inline boot `<script>` (head) precedes the body main module script; `src/ui/i18n.ts` is a pure-additive diff (prefetch helper + auto-fire; `t()`/`setLanguage`/`ensureLocaleLoaded`/`tableFor` UNCHANGED, `t()` stays synchronous); `npx tsc --noEmit` clean; `npm test` **225 files / 1982 passed / 9 skipped** (+7 cases from this QA). **Throttled (Slow-4G + 4x CPU, median of 3) network trace re-run via `vite preview`:** english = **0** non-en chunk requests; stored es = **exactly 1** request, **High** priority, **NO double-fetch**, starts **1272ms** (before main starts at 1458ms), finishes **2571ms**, **noWaterfall every run**; the `?lang=es` no-link baseline waterfalls (locale starts 21203ms only after main finishes ~20348ms, finishes **22957ms**). **The `<link>` cuts locale-ready ~22957ms -> 2571ms (~23.0s -> ~2.6s)** - re-confirms the recorded ~22965 -> ~2539 figure. Mobile (390x844 @3x): stored-es first-paint **CLS 0.0729 (<0.1 good)**, fully localized (`lang=es`, "JUGAR"/"En Línea"). **Observation (not a Phase 4 effect, not a regression):** this run measured english CLS **0.1885** (> 0.1) - the homepage marketing-copy/image settling under throttle (variable run-to-run; the recorded prior run saw ~0). Phase 4 adds only a `<head>` `<link>` (zero rendered DOM) so it cannot raise CLS; homepage layout-stability is outside this i18n packet. **TTI delta vs the no-preload baseline: locale-ready ~2.6s with the link vs ~23.0s without (the link is the only fix for the runtime-keyed waterfall; the prefetch alone lives inside main and cannot fix it).** **Fixes applied this pass (test/comment only, separate commit from this doc - no shipping-runtime change):** (1) added a `templateModulepreload` integration test (the FS orchestrator the Vite plugin calls, previously only transitively covered - now writes a tmp manifest+loaders+index.html fixture, asserts the resolved map + the in-place rewrite + the returned htmlPath/manifestPath, plus a fail-closed missing-chunk throw); (2) covered `joinBase`'s untested non-trailing-slash branch (a `localeChunkMap(..., '/app')` case); (3) de-vacuumed the `injectLocaleChunkMap` "valid JSON" assertion (now parses the literal extracted from the ACTUAL output); (4) loosened the brittle exact-string index.html contract matches to intent-level regex; (5) added 4 `prefetchLocale` unit tests (no-op for en/resident; fires the loader exactly once for non-en; swallows a rejected prefetch with retry still possible) in `tests/i18n_lazy_loader.test.ts`; (6) fixed the stale "DO NOT wait for load" comment in `scripts/i18n_modulepreload_trace.mjs`. **Deferred NICE-TO-HAVE followups (none blocking):** (a) boot-script preloads only the STORED locale, so a returning visitor whose stored locale differs from a `?lang=` override gets a wasted preload for the stored chunk while the `?lang` chunk still waterfalls post-main - DEFERRED with rationale: making the boot script honor `?lang` would ALSO put a `<link>` on the trace's `?lang=es` no-link baseline, collapsing the link-vs-no-link comparison; the runtime still loads the `?lang` locale correctly (just without the head-link speedup). (b) wire the two E2E trace/mobile scripts into an npm alias / release checklist - DEFERRED to Phase 5 (CI/hygiene is Phase 5's domain). (c) optional structural prototype-key hardening of the boot script (`Object.create(null)` / `hasOwnProperty`) - the existing `typeof href !== 'string'` guard already fully neutralizes prototype keys, so defense-in-depth only. (d) CSP: the new inline `<script>` needs no repo change (no CSP exists here; the origin nginx CSP lives in the ansible-scripts deploy repo alongside the pre-existing inline gtag/JSON-LD scripts) - track a hash/nonce there if a strict CSP is ever rolled out. **NEXT: run `docs/i18n-scaling/lazy-locales/phase-05-artifact-ci-hygiene.md` (Phase 5 - artifact / CI / determinism hygiene).** PR target `release/v0.10.0`; branch NOT pushed - confirm with the maintainer before pushing.
- Phase 4: DONE 2026-06-18 (see the Phase 4 deliverables/acceptance section above and the Phase 4 QA note).
- Phase 5: DONE 2026-06-18. Artifact/CI/determinism hygiene; zero runtime/bundle change; resolved-table SHA UNCHANGED `3254ea95..`. `git rm --cached` + gitignore the ~4.75 MB `src/ui/i18n.status.json` (regenerated by `pretest` / the new CI `npm run i18n:gen` step); NEW `.gitattributes` marks both `i18n.resolved.generated/**` dirs `linguist-generated`; NEW `i18n:gen` package script (build+admin+scan); a `Generate i18n artifacts` step added to BOTH CI jobs (after `npm ci`, before tests; two-tier gate structure untouched). NEW `tests/helpers/i18n_determinism.ts` `assertDeterministic` double-generates into perturbed-env (`TZ`/`LC_ALL`/temp-path) temp dirs via `I18N_OUT_DIR` and asserts byte-identity (mutation-proven non-vacuous); it REPLACED the status.json committed-bytes reproducibility sub-suite (swapped freshness for determinism) and ADDED a determinism check to the still-committed resolved dir (which keeps its `git diff` freshness check). NEW committed `src/ui/i18n.status.summary.json` (counts + per-locale rollup + `universeHash`, no per-key bodies) restores the audit trail and is cross-checked by the registry test. Pre-req: added the `I18N_OUT_DIR` override to `scripts/i18n_scan.mjs` (it had hardcoded `OUT_PATH`). Gates: fresh-clone-sim PR-tier `npm test` 1986 pass; release-tier 1994 pass + RED on a synthetic pending row; `i18n:hash --check` SHA unchanged; tsc clean; `git status` clean with no megabyte file tracked. Reviews `privacy-security-review` + `qa-checklist` both PASS (0 BLOCKING / 0 SHOULD-FIX); 3 NICE-TO-HAVE doc/DX hardenings applied. **KEY GOTCHA:** a gitignored file can still NEED a runtime read - `status.json` is gitignored yet read by 3 tests/scripts at runtime; `pretest` (and the CI `i18n:gen` step) regenerate it before any read, so content readers stay valid; only the COMMITTED-BYTES `git diff` reproducibility check breaks on a gitignored file and was the one swapped for `assertDeterministic`. (Phase 5 QA COMPLETE - see below.)
- Phase 5 QA: COMPLETE 2026-06-18 (Opus 4.8, max effort). Verdict **PASS-WITH-FOLLOWUPS (all APPLIED)**. Re-verified all four STOP rules independently in an isolated worktree: fresh-clone green with `status.json` ABSENT pre-build (PR-tier 1986 pass); release gate **teeth** = synthetic pending row -> RED on `pending===0`, GREEN at PR tier (same row); determinism gate **bite** = forced TZ-dependent emit -> RED ("non-byte-identical … perturbed-env runs"), GREEN after revert; SHA UNMOVED `3254ea95..` before/after regen + after build; `git status` clean post-build (no megabyte file tracked); `status.json` untracked yet present on disk (4.7 MB regenerated). Four review agents: `privacy-security-review` PASS (CI is static `run:`, `permissions: contents: read` preserved, no network/secrets/elevation), `qa-checklist` PASS (28 PASS / 0 FAIL; independently re-derived `universeHash`), dead-code PASS (ZERO orphaned committed-bytes check on `status.json`; freshness repointed to `summary.json`), test-coverage **PASS-WITH-FOLLOWUPS** (1 SHOULD-FIX). **Follow-ups APPLIED (commits `b83af46` test, `9f3414d` ci - separate from this doc):** (1) SHOULD-FIX - `assertDeterministic` gate extended to the admin resolved generator `scripts/i18n_admin_build.mjs` (game build + scanner were gated, admin twin had only the `git diff` freshness check; it passes => admin generator is genuinely deterministic - latent coverage gap, not a live bug). (2) committed non-vacuity teeth for the release-tier `pending===0` gate. (3) pinned `process.execPath` in the generator-invoking freshness checks. (4) CI `Verify committed i18n artifacts are fresh` fast-fail step in both jobs (validated: passes clean, catches drift, excludes gitignored `status.json`). Post-fix: PR-tier **1988**, release-tier **1996** (+2 each), tsc clean, build green (main gzip **720.04 kB** unchanged). **Deferred (none blocking):** OS-level perturbation effectiveness unasserted (mitigated by the unconditional temp-path axis); pinning `actions/checkout@v6`/`setup-node@v6` to SHAs (pre-existing, repo-wide, out of scope - separate change). **NEXT: run `docs/i18n-scaling/lazy-locales/phase-06-en-split.md` (Phase 6).** PR target `release/v0.10.0`; branch NOT pushed - confirm with the maintainer before pushing.
- Phase 6: _pending_
