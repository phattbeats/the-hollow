# progress.md - i18n Scaling

## Status table
| Phase | Status | Started | Completed |
|---|---|---|---|
| 0 - Layer rename (pre-packet) | DONE | - | (on branch `refactor/i18n-phase-naming`) |
| 1 - Foundation & split | DONE | 2026-06-16 | 2026-06-16 |
| 1 QA | DONE (PASS) | 2026-06-16 | 2026-06-16 |
| 2 - Resolved artifact | DONE | 2026-06-16 | 2026-06-16 |
| 2 QA | DONE (PASS) | 2026-06-16 | 2026-06-16 |
| 3 - Flatten overlays | DONE | 2026-06-16 | 2026-06-16 |
| 3 QA | DONE (PASS) | 2026-06-16 | 2026-06-16 |
| 4 - Dialect inheritance | DONE | 2026-06-16 | 2026-06-16 |
| 4 QA | DONE (PASS) | 2026-06-16 | 2026-06-16 |
| 5 - Status registry | DONE (2026-06-16) | scanner + registry + views | pending set empty (still dense) |
| 5 QA | DONE (PASS) | 2026-06-16 | 2026-06-16 |
| 6 - Unlock + two-tier CI | DONE | 2026-06-16 | 2026-06-16 |
| 6 QA | DONE (PASS WITH FIXES) | 2026-06-16 | 2026-06-16 |
| 7 - Release fill tooling | DONE | 2026-06-16 | 2026-06-16 |
| 7 QA | DONE (PASS WITH FIXES) | 2026-06-16 | 2026-06-16 |
| 8 - Admin catalog | DONE | 2026-06-16 | 2026-06-16 |
| 8 QA | DONE (PASS WITH FIXES) | 2026-06-16 | 2026-06-16 |
| 9 - Pseudo-locale (optional) | DONE | 2026-06-16 | 2026-06-16 |
| 9 QA + whole-packet QA | DONE (PASS-WITH-FOLLOWUPS) | 2026-06-16 | 2026-06-16 |
| Packet teardown | GATED on explicit user confirmation | | |

**PACKET COMPLETE (2026-06-16):** all 9 phases DONE (Phase 9 optional, delivered); each phase
QA'd; whole-feature QA verdict PASS-WITH-FOLLOWUPS (see Phase 9 QA below). Core goal met:
an English-only PR passes the PR tier; the release tier blocks until the 14-locale fill lands.
Deferred follow-ups (do NOT lose on teardown): the en_XA-surfaced hard-coded literals (Phase 9
section), the OPEN RFC 9.6 release-fill ownership / API-key item, and the admin chat-filter
pending fill (456 rows) that the release gate is correctly holding.

## Deliverable checklists

### Phase 1 - Foundation & monolith split
- [x] Authoritative nested `en` + `Leaves`/`TranslationKey`/`DeepPartial` machinery extracted to `src/ui/i18n.en.ts`
- [x] `src/ui/i18n.ts` is the thin runtime; ALL public exports unchanged (`t`, `tOptional`, `hasTranslation`, formatters, `getLanguage`/`setLanguage`, `supportedLanguages`, types)
- [x] Locale data split along seams into separate files (`src/ui/i18n.locales/<code>.ts`, 13 files; behavior-preserving; still nested `: typeof en`)
- [x] `scripts/i18n_resolved_hash.mjs` + committed `src/ui/i18n.resolved.sha256` baseline (`i18n:hash` npm script)
- [x] `tests/i18n_resolved_equivalence.test.ts` asserts the resolved table matches the baseline
- [x] tsc clean; full localization suite green (1239/1239); resolved table byte-identical to pre-change

Commits: `573bd5a` (extract en base + types), `20e8cca` (thin runtime + locale split), `d918244` (byte-equivalence baseline + gate).
Baseline: SHA-256 `d9db528bea1c7a1e02835c4d3edb3fabcee3687aad2186608f1f1d2ac83b3b9b`, 1,584,856 bytes (see state.md for why this differs from the doc's stale 1,583,881).

### Phase 2 - Dense resolved artifact - DONE (2026-06-16)
- [x] `scripts/i18n_build.mjs` overlays locales onto `en`, emits `src/ui/i18n.resolved.generated.ts` (nested, `: EnTranslations` = `typeof en`, do-not-edit banner)
- [x] Client imports the generated artifact through the runtime; `t`/`tOptional`/`hasTranslation`/`translationValue` repointed at the dense table. (Admin: N/A this phase - `src/admin/` is independent of `src/ui` and ships its own flat DICT; the main table is not an admin consumer, so the phase-doc "admin imports the generated artifact" line is a no-op here. Admin catalog is Phase 8.)
- [x] `i18n:build` wired into `npm run build` (before vite) + `pretest`; reproducibility `git diff --exit-code` test green (plus a tracked-file assertion so the gate is not vacuous on an untracked artifact)
- [x] Resolved table byte-identical to Phase 1 output (SHA-256 `d9db528..` unchanged)

Commits: `3f1ed8d` (build script + generated dense table + `EnTranslations` + wiring), `a92ff37` (runtime consumes the dense table), `ffb40e5` (reproducibility test).
Validation: `tsc --noEmit` clean; targeted suite 77/77 (localization_fixes + localization_coverage + server_i18n + i18n_resolved_equivalence); byte-equivalence SHA `d9db528bea1c7a1e02835c4d3edb3fabcee3687aad2186608f1f1d2ac83b3b9b` (14 locales, 1,584,856 bytes); regeneration byte-identical; `npm run build` clean (client + admin).
Bundle delta (locale data now fully inlined, losing cross-locale spread-sharing): main bundle gzip 966.77 -> 1120.64 KB (+153.9 KB, +15.9%); admin bundle unchanged. A naive repoint balloons +479.8 KB gzip because the `gameStrings` re-export dragged the full ~1 MB `i18n.en` base in alongside the inlined table; sourcing `gameStrings` from the generated `en.game` recovers ~326 KB gzip of pure duplication. The residual +154 KB is inherent to a dense table (no cross-locale sharing) plus `world_entity_i18n` entity data being inlined in the table AND still bundled for the `hud`/`entity_i18n` resolver - see state.md gotchas.

### Phase 3 - Flatten non-English locales - DONE (2026-06-16)
- [x] 13 main-table non-English locales converted to flat dotted-key overlays in `src/ui/i18n.locales/<lang>.ts` (`Record<string, string>`, 1925 keys each, dense). Produced by the one-time migration `scripts/i18n_flatten_locales.mjs`; they no longer import `i18n.en`/`world_entity_i18n` (standalone flat literals)
- [x] `en` stays nested + authoritative (`src/ui/i18n.en.ts` untouched); generator unflattens each overlay onto a deep copy of `en` and emits the byte-identical dense artifact
- [x] Overlays still dense (every key present); temporary key-completeness test passes (`tests/i18n_flat_overlay_dense.test.ts`, exact key-set vs `flatten(en)` + non-empty-string values)
- [x] Resolved table byte-identical (SHA-256 `d9db528..` unchanged; generated-file blob `90b4326..` unchanged; `git diff --exit-code` clean)
- [~] DEVIATION (deliberate, sign-off via maintainer "do whatever is best"): the two island files were NOT flattened. `world_entity_i18n.ts` is all-string but has ZERO runtime path-indexers and its non-English data is superseded by the inlined overlays (flattening it would be dead-code churn); its `.en` slice still feeds nested `en`. `talent_i18n.ts` is a SEPARATE channel (not in the byte-frozen table), read at runtime by `hud.ts`/`tTalent`, and its `localeText` has FUNCTION-valued leaves (`chooseOne`/`specDescription`/`grant`/`increase`/`reduce`) that cannot become a flat string map; `titleOverrides` is already a flat string map. Both deferred to Phase 4 (which already owns `world_entity_i18n` dialect/overlay semantics).

Commits: `refactor(i18n): flatten non-English locales to dotted-key overlays` (contract `i18n_flatten.mjs` + build overlay path + migration + 13 overlays), `test(i18n): assert flat overlays match en leaf set and stay dense`, this doc commit.
Validation: `tsc --noEmit` clean; targeted suite 31/31 (flat-overlay-dense + resolved-equivalence) and 77/77 (localization_fixes + localization_coverage + server_i18n + i18n_resolved_equivalence); full suite 1268/1268; `npm run build` (client+admin) + `build:env` + `build:server` clean; byte-equivalence SHA `d9db528..` (14 locales, 1,584,856 bytes) unchanged; regeneration byte-identical. Main bundle gzip 1120.64 KB (unchanged from Phase 2 - overlays are build-time source, not bundled).
Reviews (coverage mode, whole diff): privacy-security-review PASS (no BLOCKING/SHOULD-FIX), cross-platform-sync PASS (no BLOCKING; islands-left-nested confirmed correct), correctness review PASS (round-trip proven total in both directions vs git HEAD; could not break it). Hardening applied from review NICE-TO-HAVEs: `flatten`/`unflatten` now throw on a dotted key segment or a prefix collision (fail-loud instead of silent corruption); neither is reachable in today's data.
Type note: overlays are typed `Record<string, string>`, not `Record<TranslationKey, string>` - `TranslationKey = Leaves<typeof en, 5>` stops at depth 5, but the deepest real leaves (`entities.quests.<id>.objectives.0.label`, `entities.zones.<id>.pois.<n>.label`) are 6 segments deep, so they are not in `TranslationKey`. Key validity is enforced by the temporary completeness test + the byte gate instead.

### Phase 4 - Dialect inheritance dedup - DONE (2026-06-16)
- [x] `es_ES` overlay carries only divergences from `es` (72 keys); `fr_CA` only from `fr_FR` (37 keys); `en_CA` thin alias of `en` (3 keys: `classDetails.labels.armor`+paladin/druid lore "armour"/"defence")
- [x] Resolver applies base then dialect overlay; base declared data-driven via `DIALECT_BASE` map in `scripts/i18n_build.mjs` (`es_ES`->`es`, `fr_CA`->`fr_FR`, `en_CA`->`en`). Resolve order for a dialect: nested `en` -> base overlay -> dialect overlay; omitted keys fall through to base, then English.
- [x] `{} as WorldEntityTranslations` casts removed (`src/ui/world_entity_i18n.ts` reduced to English-only; the dead non-English datasets + `makeLocaleWorldEntities` deleted - they had zero runtime consumers after the Phase 3 inline). The equivalent `{} as TalentLocaleText` casts in `src/ui/talent_i18n.ts` `localeText` replaced by explicit dialect aliases over a `satisfies`-typed base record (no cast, no post-hoc reassignment).
- [x] Resolved table byte-identical (SHA-256 `d9db528..` unchanged; generated file regenerates byte-identical, `git diff --exit-code` clean)

Commits: `feat(i18n): add declared-base dialect overlay resolution`, `refactor(i18n): dedup es_ES/fr_CA/en_CA to divergence-only overlays`, `refactor(i18n): remove unsafe world-entity and talent dialect casts`, this doc commit.
Validation: `tsc --noEmit` clean; targeted suite 126/126 (localization_fixes + localization_coverage + server_i18n + i18n_resolved_equivalence + i18n_flat_overlay_dense + i18n_overlay_key_membership); full suite 1290/1290; `npm run build` (client+admin) + `build:env` + `build:server` clean; byte-equivalence SHA `d9db528bea1c7a1e02835c4d3edb3fabcee3687aad2186608f1f1d2ac83b3b9b` (14 locales, 1,584,856 bytes) unchanged; regeneration byte-identical. Main bundle gzip 1,120.64 KB unchanged (overlays are build-time source; the deleted world_entity non-English data was not in the client bundle).
DESIGN NOTE (world_entity keep-vs-remove): the Phase 3 handoff left `world_entity_i18n.ts` non-English data dead (overlays superseded it; `tEntity` resolves via the resolved table, not this object; zero `worldEntityText[lang]` indexers). Chose DELETE (English-only) over re-establishing it as the single source (which would reverse Phase 3's full-inline) - maintainer-confirmed. The flat overlays are now the single non-English entity-name source; `worldEntityText.en` still feeds nested `en`.
OUT OF SCOPE (untouched, deferred): talent `titleOverrides` es_ES/fr_CA full blocks left as-is (live, not byte-gated, no cast there; deduping them was not in the phase cadence); no sparseness for non-dialect locales (Phase 6); no locale registry (Phase 5); no `t()`-miss change; admin DICT untouched (Phase 8).

### Phase 5 - Status registry + scanner - DONE (2026-06-16)
- [x] `scripts/i18n_scan.mjs` (no LLM/network) walks `en` + matcher (`sim_i18n`/`server_i18n` DICTs) + admin keys, computes `srcHash` (English text + sorted placeholders via the shared `scripts/i18n_hash.mjs`), writes `src/ui/i18n.status.json`. 2278 keys x 13 locales; scoped `<main|sim|server|admin>:<key>` rows; pure function of source (no prior-output read), deterministic ordering (scope rank, then key).
- [x] Registry states: `translated` (with `srcHash`==`enHash`, `by:human`), `pending`, `blocked` (with `reason`). Cognates seeded as per-key per-locale `blocked` rows; the v0.7 slash-command source strings as a `blockedSource` list (channel `sim`). Seed in `scripts/i18n_blocked_seed.mjs` (byte-faithful extraction of the former in-test Sets; now the canonical hand-maintained source).
- [x] `COPIED_ALLOW` (43) / `ALLOW_V07_SLASH` (105) are now VIEWS over the registry in `tests/localization_fixes.test.ts` (literal Sets removed); H3b copied-English + S3 drift guards still bind unchanged.
- [x] `i18n:scan` wired into `build` + `pretest` (after `i18n:build`); reproducibility + registry-in-sync test `tests/i18n_status_registry.test.ts` green (coverage of full universe, independent enHash re-derivation, pending-empty, blocked load-bearing/no-over-allow, placeholder-in-hash sensitivity, regenerate-then-`git diff --exit-code`). Teeth proven by mutation (drop a key -> coverage fails; forge a non-cognate blocked row -> over-allow fails).
- [x] `pending` set empty at this stage (everything still dense after Phase 4). Resolved-table byte-equivalence unchanged (SHA `d9db528..`).

### Phase 6 - The unlock: relax types + two-tier CI - DONE (2026-06-16)
- [x] Flat overlays relaxed to `Partial<Record<TranslationKey,string>>` (sparse legal). `TranslationKey` deepened to `Leaves<typeof en, 6>` so it reaches the depth-6 leaves (objectives/pois `.label`); measured no tsc cost. Temporary dense test `i18n_flat_overlay_dense.test.ts` deleted; permanent subset guard `i18n_overlay_key_membership.test.ts` kept (catches entity-id typos the template-literal `${string}` key members accept).
- [x] `t()` throws on an untracked key in dev/test (returns raw key on release), renders English for a registry-`pending` key on **non-release builds only**, and HARD-FAILS on a pending key on a release build. `tOptional`/`hasTranslation`/`translationValue` UNCHANGED (return null on miss, so `tEntity` fallback is preserved). Release flag: `isReleaseBuild()` = `I18N_RELEASE=1` (tests/release step) OR `import.meta.env.PROD` (real Vite build), read lazily on the cold path. `scripts/i18n_build.mjs` emits a per-locale `pending` list (dialect-aware, mirrors `i18n_scan.mjs`; empty while dense).
- [x] `.github/workflows/ci.yml` split by ref into two jobs in one file: `pr-gate` (pull_request / push non-release / dispatch; `npm test` without the tier flag) vs `release-gate` (push `release/**`; `I18N_RELEASE_TIER=1 npm test`). Routing proven: exactly one job per event.
- [x] S3 guard split into `s3_registered` (PR) + `s3_localized` (release, all 14 locales), preserving the `sim.ts`/`hud.ts` source scraping; H3b + coverage quest/talent copied-English + empty-`pending` (registry AND t-behavior) moved to release-only via `I18N_RELEASE_TIER`.
- [x] Proof (5 gate cases, all confirmed directly): English-only sample key (pending) PASSES PR tier (118 passed/7 skipped) and renders English on non-release; same key FAILS the release tier (empty-pending blocks); untracked key THROWS in dev/test and FAILS the PR gate (tsc `TS2345`); pending key HARD-FAILS on a release build (doMock test). Full suite: PR tier 1282 passed/7 skipped; release tier 1289 passed. `npm run build`/`build:env`/`build:server` clean.

Commits: `3307adb` (relax overlay types), `1ece3dc` (t() miss + pending emit), `8a6a473` (CI split), `9beb071` (S3 split + content re-home), `dcd07bd` (gate-proof fix: t-behavior empty-pending -> release tier), this doc commit.
BASELINE NOTE: `src/ui/i18n.resolved.sha256` is UNCHANGED (`d9db528..`). The dense tree's resolved table does not change in Phase 6 - overlays are NOT depopulated, so `pending` is empty and nothing English-fills differently; adding the generated `pending` export does not touch `translations`, so the hash is identical. The phase doc's STEP 3 expectation that the baseline moves is conditional on actual depopulation (Phase 7); it does not happen while overlays stay full. (Verified: regen byte-identical, hash `d9db528..`.) This is a phase-DOC defect, not a code defect - both the correctness and cross-platform review lanes independently confirmed the no-baseline-change is correct. NOTE FOR PHASE 6 QA: do NOT flag the unchanged hash as a missed STEP-3 step.

Reviews (coverage mode, whole diff `30e070e..HEAD`, adversarial-verify Workflow + a direct re-run of the privacy lane): **0 BLOCKING / 0 HIGH / 0 MEDIUM / 0 LOW, 10 INFO.**
- `privacy-security-review`: NON-BLOCKING. "English cannot leak to a translated player on any release path" - `import.meta.env.PROD` is reliably `true` in the `vite build` client (no mode/define override); `I18N_RELEASE` only ADDS release-ness; the pending branch throws before `interpolate` on release; with `PENDING_TOTAL===0` the hot path is byte-identical to before; no new non-client leak path. Fail-closed by design.
- `cross-platform-sync`: NON-BLOCKING. S3 split preserves the full `sim.ts`/`hud.ts` scraping + recognition coverage (recognition is locale-independent, so `s3_registered` in `en` still catches an unregistered emit); the build `computePending` and scan `providedByLang` produce byte-identical pending sets (verified dense AND synthetic-sparse, dialect-aware); determinism + `src/sim`/`server` import isolation intact; baseline `d9db528..` unchanged.
- correctness/requirement-gaps: NON-BLOCKING. `t()` correct on every branch; no existing runtime caller breaks (no template-literal main-`t()` keys; entity ids use `tOptional`, unchanged; `index.html` keys newly pinned); CI routes exactly one job per event, release is a true superset; membership/pending/English-fill invariants each fail correctly when a violation is planted.
- Deferred INFO (optional hardening, not required for Phase 6): (1) extract the duplicated `DIALECT_BASE`/`LOCALES`/`isPresent` shared by `scripts/i18n_build.mjs` (computePending) and `scripts/i18n_scan.mjs` (providedByLang) into one module so a future edit cannot silently break their lockstep - better yet, when `pending` goes live in Phase 7 add a test cross-checking build-pending == registry-pending; (2) optionally have `readTranslationKey` validate against the table (return null on a miss) so a future runtime-injected `data-i18n` attribute degrades to no-op instead of throwing in dev (no such injection exists today).

### Phase 7 - Release fill worklist + docs - DONE (2026-06-16)
- [x] `scripts/i18n_fill_worklist.mjs` (`i18n:worklist`) emits per-language `pending` delta; each auto-fillable entry is `{ scope, key, english, placeholders, siblings }`, one batch per language under `docs/i18n-scaling/worklist/<lang>.json` (gitignored generated artifact) + a `manifest.json`. DATA-ONLY (no network, no LLM, no dynamic outbound import; esbuild-bundles local source to a `data:` URL exactly like `i18n_scan.mjs`). Deterministic (language order = `registry.locales`; entries sort by scope rank then key) and a zero-write no-op on an unchanged repo (content-hash cached via the manifest `inputHash`). A staleness guard refuses to run if the registry drifted from source (re-scan first).
- [x] Locked-terms glossary shipped with EVERY batch. Source: `scripts/i18n_glossary.json` (hand-maintained, like `i18n_blocked_seed.mjs`): `verbatim` brand ("World of ClaudeCraft", never translated) + `categories` key-patterns for the 9 class names, ability names (`entities.abilities.*.name`), zone names, dungeon names. The tool expands the patterns against `en` and resolves each term's established localized form per language (own overlay -> dialect base -> English, mirroring the build), so terminology does not drift.
- [x] Blocked prose is segregated to a `humanRequired` section and CANNOT reach `autoFillable` (the stopping rule). Classification is blocked-by-default: `entities.*` / `classes.*` / `classDetails.lore.*` / `seo.*` are prose (checked before the chrome allow-list, so `classDetails.lore.*` is not auto-fillable via the `classDetails.` chrome prefix); any namespace not on the chrome allow-list also defaults to `humanRequired`. A `main()`-time assertion re-checks. (CJK talent names live in `talent_i18n.ts`, a non-registry channel, so they are structurally unreachable by the tool.)
- [x] Contributor workflow documented in `src/ui/CLAUDE.md` (stale Phase-1 "add to every locale object" text replaced with the sparse-overlay model + the add-to-en-only flow); maintainer release-fill workflow + glossary + the OPEN RFC 9.6 ownership/API-key item in `docs/i18n-scaling/translation-workflow.md`. New unit test `tests/i18n_fill_worklist.test.ts` (10 cases) locks the classification stopping rule, glossary expansion, and sibling determinism.
- [x] Worklist round-trip PROVEN: depopulated 3 de_DE keys (1 chrome `loading.worldProgress` w/ `{done}`/`{total}`, 2 prose `classes.mage` + `entities.quests.q_wolves.title`) -> `i18n:scan` (pending=3) -> `i18n:worklist`: chrome key in `autoFillable` with english+placeholders+6 siblings + the glossary; both prose keys in `humanRequired`, absent from `autoFillable`. Filled the chrome key -> re-scan (pending=2, auto=0). Restored fully -> re-scan (pending=0); `i18n.status.json` byte-identical to committed (git diff clean); worklist removed the stale batch (`inputHash` back to the empty-baseline `25e8af71`).

Validation: `tsc --noEmit` clean; `tests/i18n_fill_worklist.test.ts` (10) + named suite `localization_fixes` + `localization_coverage` + `server_i18n` = 79 passed / 5 skipped (the 5 are the pre-existing release-tier gates at the PR tier); `npm run build` (client + admin) clean, main bundle gzip 1,121.01 KB (unchanged vs Phase 6, no balloon); worklist re-run twice = byte-identical no-op. Generated files (`i18n.status.json` / `i18n.resolved.generated.ts` / media manifest) regenerate byte-identical after build (git clean). Resolved-table baseline UNTOUCHED (`d9db528..`): Phase 7 adds tooling only and does not depopulate any overlay, so `pending` stays 0 and the resolved table is byte-identical.
Reviews (coverage mode): privacy-security-review NON-BLOCKING (0 critical/warning; data-only/no-network/no-secrets/gitignored-output confirmed; stopping rule holds against the real 1,925-key en space); cross-platform-sync NON-BLOCKING (resolution constants `DIALECT_BASE`/`isPresent`/placeholder helper/`sourceModule`/DICT exports byte-identical to scanner+build; pending read from registry, not re-derived; `src/sim`/`server` untouched, zero runtime imports of the tool); correctness/requirement-gaps NON-BLOCKING (all 7 STEP-5 acceptance items verified with live proofs; staleness guard bites both directions; only finding was this STEP-6 doc bookkeeping, now done).
Commits: see Phase 7 cadence in state.md additions-log row 7.

### Phase 8 - Admin catalog into the model - DONE (2026-06-16)
- [x] `src/admin/i18n.ts` brought under the overlay + registry + release-gate model. One-time migration `scripts/i18n_admin_split.mjs` split the monolithic dense `DICT` into a flat authoritative English base `src/admin/i18n.en.ts` (181 keys) + 13 flat per-locale overlays `src/admin/i18n.locales/<lang>.ts`. New build `scripts/i18n_admin_build.mjs` (`npm run i18n:admin`, wired into `pretest` + `build` BEFORE `i18n:scan`) resolves them into the dense generated `src/admin/i18n.resolved.generated.ts` (each locale `: AdminTranslations` = typeof en, dialect-aware es_ES->es/fr_CA->fr_FR/en_CA->en, English-fill for gaps, `pending` map). Runtime `src/admin/i18n.ts` now reads the resolved table, re-exports a dense `DICT`, and its `t()` adopts the game's pending/release/untracked policy (English-fill on non-release, hard-fail on a pending key at release, throw-on-untracked in dev / raw key on release). English-only admin PRs are legal at the PR tier; 14-locale completeness is gated at release.
- [x] Admin keys TRACKED in the registry. `scripts/i18n_scan.mjs` admin scope moved from the dense `addDictScope('admin', adminDICT)` to a new sparse, dialect-aware, blocked-seed-aware `addSparseScope('admin', adminEn, adminOverlays)` reading the SPARSE source (en base + overlays), so an omitted admin key naturally yields `pending` exactly like a `main` key. The 181 existing admin rows are byte-identical (0 state flips; 38 blocked cognates preserved); `scripts/i18n_fill_worklist.mjs` now surfaces admin pending keys per language (admin is mechanical chrome -> autoFillable) and points the fill at `src/admin/i18n.locales/<lang>.ts`.
- [x] Hardcoded admin strings localized (the chat-filter / chat-moderation operator surface was previously raw English, not keys): the `window.alert(...)` at `src/admin/main.ts:401`, the two chat-mute confirm dialogs, the fallback alerts, the entire chat-filter + chat-moderation render block in `src/admin/tables.ts` (incl. the `title="Remove"` attribute + the `add a soft/hard word` placeholders), and the `admin.html` "Chat Filter" tab (now `data-i18n="nav.chatFilter"`). 38 new keys added English-only to the admin en base (overlays omit them -> `pending`, filled at release via the worklist), per the post-unlock contributor workflow. Reused existing translated keys where English matched (dialog.action, detail.reason/lengthHours, report.colTime/colChannel/colMessage, alert.actionFailed).
- [x] Admin renders all 14 locales (dense resolved table, 219 keys each); admin build clean; admin stays a SEPARATE bundle (`dist/assets/admin-*.js` ~33 KB gzip, imports only its own resolved table - verified no game locale table / `entities.*` / `abilityUi` / `classDetails` strings in the admin chunk). Non-client-consumer audit (RFC §9.7) PASSES (see state.md): `index.html` hreflang + `data-i18n-content` meta are game-side, pinned to real `en` leaves, game `pending` empty at release; `admin.html` static `<title>`/`data-i18n` defaults are overwritten by `localizeStatic()` and the keys are real admin en keys (new test); admin `pending` keys English-fill on non-release and the release gate requires `pending=0`, so no non-client surface can leak pending-English to a real user at release.

Validation: `tsc --noEmit` clean; full suite 1318 passed / 7 skipped at the PR tier (130 files); the release tier (`I18N_RELEASE_TIER=1`) fails ONLY on the two intended gate checks - `registry.counts.pending` (456 = 38 new admin keys x 12 non-en/non-en_CA locales) and the admin H3b copied-English on the English-filled pending keys - proving "14-locale completeness gated at release" with real data (no collateral failures; the 181 existing admin keys + all main/sim/server rows pass at release tier). `npm run build` clean (game + admin entries; admin a separate 175 KB / 33 KB-gzip chunk). New tests in `tests/i18n_admin_catalog.test.ts` (two-tier gate via a synthetic pending key, bundle isolation, overlay-key membership, admin.html data-i18n keys are real admin en keys, resolved-table reproducibility). Game resolved table + `i18n.resolved.sha256` UNTOUCHED (`d9db528..`): Phase 8 is admin-only.
Reviews (coverage mode): privacy-security-review NON-BLOCKING (escapeHtml preserved 33->33 on all player-controlled values; no secret/PII in new strings or the generated table; admin auth gate untouched; no new untrusted innerHTML sink); cross-platform-sync NON-BLOCKING (registry diff is 38 admin rows added, ZERO main/sim/server rows changed; admin bundle isolated; `src/sim`/`server`/IWorld/wire untouched; `DIALECT_BASE` identical across scan/build/worklist); qa-checklist NON-BLOCKING (RFC §9.7 holds; admin entry separate; resolved table dense for all 14); fresh correctness NON-BLOCKING (181 existing admin rows byte-identical, build reproducible, all `t()` keys exist + placeholders match, acceptance met; one optional cleanup applied: worklist now reads the admin source en for symmetry with the scanner).
Commits: see Phase 8 cadence in state.md additions-log row 8.

### Phase 9 - `en_XA` pseudo-locale (optional) - DONE (2026-06-16)
- [x] Generated accent/bracket pseudo-locale over every `en` leaf, selected via `?lang=en_XA`. Shared deterministic transform `scripts/i18n_pseudo.mjs` (fixed 1:1 ASCII->Latin accent map; brackets each leaf; preserves any `{...}` token EXACTLY; emoji/non-ASCII pass through). Both build scripts emit a separate `export const en_XA` into their generated tables (`scripts/i18n_build.mjs` -> `src/ui/i18n.resolved.generated.ts`; `scripts/i18n_admin_build.mjs` -> `src/admin/i18n.resolved.generated.ts`). en_XA is GENERATED only, never in `src/ui/i18n.locales/`.
- [x] Excluded from `supportedLanguages`, hreflang, and the release gate - BY CONSTRUCTION: en_XA is NOT a member of `translations`, so `supportedLanguages = Object.keys(translations)` omits it, the language picker (populated from supportedLanguages) omits it, `index.html` hreflang (hand-authored, 14 + x-default) is unchanged, and the scanner/registry (`supportedLanguages` minus `en`) never sees it. The byte-equivalence baseline `i18n_resolved_hash.mjs` reads `supportedLanguages` only, so the game gate SHA is UNCHANGED (`d9db528..`, 1,584,856 bytes).
- [x] Selectable via `?lang=en_XA` in DEV ONLY. Game `src/ui/i18n.ts` + admin `src/admin/i18n.ts` accept the param only when `!isReleaseBuild()` (keeping the base locale at the real `"en"`, never persisted); a `tableFor()` indirection swaps in the pseudo table on the current-language read path. The en_XA reference sits behind a static `import.meta.env.PROD` guard, so a production `vite build` TREE-SHAKES the pseudo table out of both bundles (verified: 0 signature glyphs in `dist/assets/main-*.js` + `admin-*.js`; bundles unchanged at 1,121.05 / 33.17 KB gzip).
- [x] Surfaces hard-coded literals that never became `t()` keys - PROVEN end-to-end via a live `?lang=en_XA` browser scan (login/HUD/6 windows/options/admin-login). en_XA paid for itself immediately - it surfaced real gaps the type system and the S3 guard both miss (see the surfaced-literals report below). Admin brought under the same model (Phase 8 QA deferred the admin literal-surfacing gap here).

Validation, all green: `npx tsc --noEmit` clean; `tests/i18n_pseudo_locale.test.ts` (12: generator parity/bracketing/placeholder-preservation/accent/determinism + exclusion from supportedLanguages/isSupportedLanguage/admin DICT + dev-selector-on / release-off) pass; named suite `localization_fixes` + `localization_coverage` + `server_i18n` + `i18n_status_registry` + `i18n_t_behavior` + `i18n_admin_catalog` + `i18n_pseudo_locale` all green at the PR tier (the only "failures" mid-work were the two `regenerate-then-git-diff` reproducibility checks failing against an UNCOMMITTED artifact - they pass once the regenerated tables are committed, which is the by-design committed-state gate); `npm run build` clean (game + admin), generated tables deterministic + purely additive (only the en_XA export added; 14 locales/`translations`/`pending` untouched). Resolved-table baseline UNTOUCHED (`d9db528..`). Reviews: privacy-security-review + cross-platform-sync + correctness all NON-BLOCKING (0 BLOCKING/HIGH; tree-shake-out-of-prod and exclusions independently re-verified; the new test mutation-proven to bite).

**Surfaced hard-coded literals (deferred follow-ups - per the spec, the MECHANISM ships here; FIXING these is out of Phase 9 scope):**
- TRUE i18n gaps (player/operator-visible, not via `t()`):
  1. Minimap side-button aria-labels - `src/ui/hud.ts:1386-1392`: the `sideButtons` array hard-codes English labels (Character/Spellbook/Talents/Quest Log/Map/Bags/Arena/Leaderboard/Emotes/Friends) and sets `aria-label` as `` `${label} (${key})` `` , bypassing `t()`. The `data-i18n-aria` keys exist in `index.html` but are overwritten dynamically by this literal. 10 controls.
  2. Skin/Chroma swatch aria-labels - `src/ui/hud.ts:3688` (`aria-label="Chroma"` in the char-window template), `src/ui/hud.ts:3756` (`b.setAttribute('aria-label', \`Chroma ${i+1}\`)`), `index.html:4653` (online skin row `aria-label="Chroma"`), `index.html:4692` (offline skin row `aria-label="Skin"`). No `data-i18n-aria`.
  3. Chat tip literal - `src/ui/hud.ts:451`: `this.log('Tip: type /join world or /join lfg to chat with players across the realm.', ...)` is a hard-coded English client chat message (not a `t()` key, not a matcher string).
  4. Version/build footer - `src/main.ts:180`: `` `v${__APP_VERSION__} · build ${__APP_BUILD_ID__}` `` - the literal word "build" is player-visible English (low priority; technical string).
- BY-DESIGN / acceptable (noted, not gaps): "World of ClaudeCraft" visually-hidden hero `<h1>` in `index.html` (brand verbatim, never translated per the glossary); keybind key-name labels ("esc" etc.) in the `.keybind` spans (physical key names, conventionally not localized).
- en_XA SCOPE LIMITATIONS (so a future scan is not misread): en_XA pseudo-izes only the MAIN `t()` table (and the admin `t()` table) for the current language. Sim/server matcher-channel text (`localizeSimText`/`localizeServerText`) resolves via `DICT[getLanguage()]` = `en` under pseudo, so it renders plain English and is NOT a literal (a naive scan false-positives it; item 3 above is a true literal because it bypasses BOTH `t()` and the matcher). Canvas/WebGL text (FCT, nameplates) is not DOM and is not surfaced. The admin scan covered the LOGIN screen only (no server session); it showed 0 literals there.

Commits: see Phase 9 cadence in state.md additions-log row 9.

## QA-phase checklists (fixes applied, tests added, dead code removed)
Filled in by each QA session.

### Phase 1 QA - PASS (2026-06-16)
Verdict: PASS. Issues found: 6, all NICE-TO-HAVE; fixed: 0; deferred: 6. No BLOCKING or SHOULD-FIX. No source changes were needed, so no fix commit.
- Method: 1 context loader + 5 parallel read-only audits (correctness, dead-code/cleanup, privacy-security-review, cross-platform-sync, qa-checklist), all PASS, plus a test-coverage mutation proof run directly in the main loop.
- Validation matrix, all green: `tsc --noEmit`; `vitest run` of localization_fixes + localization_coverage + server_i18n + i18n_resolved_equivalence (4 files, 75 tests); byte-equivalence gate SHA `d9db528bea1c7a1e02835c4d3edb3fabcee3687aad2186608f1f1d2ac83b3b9b` (14 locales, 1,584,856 bytes); `npm run build` (client + admin).
- Mutation proof (the gate is meaningful, not vacuous): temporarily changed one inline `es` value (`nav.home` "Inicio" to "InicioQA"); the resolved SHA changed to `e2f70bc8...`, `--check` exited 1, and both equivalence tests went red. Reverted via explicit-path `git checkout`; SHA and tests green again.
- Behavior-preserving confirmed against pre-work commit `a9a1a67`: the `./i18n` public export surface is unchanged name-for-name and signature-for-signature (only additive new export `DeepPartial`); thin-runtime function bodies (t, tOptional, hasTranslation, interpolate, formatMoney/moneyParts, getLanguage/setLanguage, formatNumber/formatDateTime) are byte-identical to the monolith; `en` still nested and authoritative; 13 locales still nested `: typeof en`; `src/sim` import invariant intact (`world_entity_i18n` imports only `../sim/data`).
- Deferred NICE-TO-HAVE for later phases: (1) shared content layers + per-locale gameStrings variants + `DeepPartial` are now `export` on `i18n.en` (necessary so locale files can spread them; NOT on the public `./i18n` barrel, so the surface stays unchanged); (2) `tsconfig` `noUnusedLocals:false` will not catch a future orphaned layer import once Phase 3 churns the locale files; (3) hash script `data:`-URL import is safe but obscures parse errors and skips the module cache (dev/CI tooling only); (4) the byte-equivalence gate covers locale data only, not the runtime function/type surface (tsc covers that); (5) cosmetic `./` vs `../` worldNames import path differs between the en base and the locale files (same module).

### Phase 2 QA - PASS (2026-06-16)
Verdict: PASS. Findings across 6 lanes: 0 BLOCKING, 1 SHOULD-FIX acted on (1 new test), the rest NICE-TO-HAVE (deferred, all pre-existing or out of scope). Stopping rule (byte-equivalence / reproducibility) never tripped. Run together with Phase 4 QA in one session per the user's request to run both.
- Method: deterministic gates run in the main loop + a 12-lane coverage-mode review Workflow (6 lanes each for Phase 2 and Phase 4: correctness, test-coverage, dead-code, privacy-security-review, cross-platform-sync, qa-checklist) with adversarial verification of every BLOCKING/SHOULD-FIX finding. Plus two main-loop mutation proofs (below).
- Validation matrix, all green: `tsc --noEmit` clean; targeted suite 137/137 across the 6 prior i18n files + the 2 new tests; byte-equivalence SHA `d9db528bea1c7a1e02835c4d3edb3fabcee3687aad2186608f1f1d2ac83b3b9b` (14 locales, 1,584,856 bytes) unchanged; regenerate + `git diff --exit-code` clean (reproducible); `npm run build` (client + admin) clean (main bundle gzip 1,120.64 KB unchanged).
- Confirmed (correctness lane): all four runtime read-paths (`t`, `translationValue`, `hasTranslation`, `tOptional` in `src/ui/i18n.ts`) read the dense generated `translations` from `./i18n.resolved.generated`; no raw `i18n.locales/*` value read remains (verified the import block + each helper body). Do-not-edit banner present + manifest-style; every locale typed `: EnTranslations`. `i18n:build` wired into `npm run build` before vite and into `pretest`. Generator uses Node built-ins + esbuild (existing devDep), no new runtime dependency. Admin N/A confirmed: `src/admin/` keeps its own independent flat DICT and consumes neither the generated table nor `ui/i18n` (the phase-doc "admin imports the generated artifact" acceptance line is a verified no-op; progress.md already records this).
- Mutation proof A - byte-equivalence + reproducibility have teeth (the gates are complementary): (1) mutated one `es.ts` source value (`nav.home` -> "InicioQA_MUTATION"); `tests/i18n_resolved_equivalence.test.ts` reproducibility test went RED (regenerate -> `git diff --exit-code` non-clean). The hash test stayed green because post-Phase-2 the hash script bundles `i18n.ts`, whose per-locale exports now come from the COMMITTED generated artifact (not raw source); so the hash gates artifact->baseline while the reproducibility test gates source->artifact - together they are complete. (2) Then mutated the committed generated artifact directly (one `"Home"` -> "HomeQA"): SHA flipped `d9db528..` -> `8004e774..` and `--check` exited 1. Both restored via explicit-path `git checkout` + regen; tree clean, SHA back to `d9db528..`.
- Fix (SHOULD-FIX, commit `0d49b25`): `tests/i18n_build_gapfill.test.ts` - the Phase 2 QA packet explicitly directs the test-coverage lane to ADD build gap-fill coverage; none existed (the prior tests assert SOURCE overlay shape or the opaque byte SHA, never the deepMerge OUTPUT by representative key). New test asserts on the generated table: a leaf a sparse overlay omits is filled from English (`en_CA.nav.home` == en "Home"); a present translated leaf is preserved (`es.nav.home` == "Inicio", not English); a present divergent override is preserved (`en_CA.classDetails.labels.armor` == "Armour", not en "Armor"); and a sparse overlay resolves to the full en leaf set (dense output, no gaps). One adversarial verifier refuted this as out-of-scope ("Phase 2 QA not started"), but that is moot - the user asked to run Phase 2 QA in this session, and the packet scopes the test; a second verifier agreed the gap is real (downgraded only the severity label).
- Deferred NICE-TO-HAVE (pre-existing / out of scope, not Phase 2 regressions): (1) the `{} as typeof <layer>En` placeholder casts + sibling non-en slices in `src/ui/i18n.en.ts` shared content layers are orphaned dead code after Phase 3 made the overlays standalone full-inline - pre-existing Phase 1/2 code, already tracked as deferred cleanup in state.md (the `en` base still spreads its own `.en` slices, so the module must stay). (2) The byte-equivalence baseline can be co-rewritten with the table and still pass green - by design (the file header states it changes only in a phase that deliberately changes output; caught by review of the `.sha256` diff). (3) Per-locale value re-exports from `i18n.ts` have no downstream value-importers - kept as stable public surface per the Phase 1 decision. (4) Pre-existing em dashes in source English copy are mirrored into the generated artifact - content issue, neither introduced nor in scope for this build-tooling phase.

### Phase 3 QA - PASS (2026-06-16)
Verdict: PASS. Findings: 4, all non-blocking (0 BLOCKING, 0 SHOULD-FIX). 2 acted on (1 new test, 1 comment correction); 2 deferred (already tracked). Stopping rule (byte-equivalence) never tripped.
- Method: deterministic gates run in the main loop + a 6-agent coverage-mode review Workflow (correctness, test-coverage, dead-code, plus privacy-security-review, cross-platform-sync, qa-checklist), each reviewing the whole diff (range `ea7addb..HEAD`). Augmented with an independent per-locale adversarial verifier (own unflatten/merge, not the project's) confirming each of the 13 locales resolves byte-identical to the committed artifact.
- Validation matrix, all green: `tsc --noEmit` clean; targeted suite 120/120 across localization_fixes + localization_coverage + server_i18n + i18n_resolved_equivalence + i18n_flat_overlay_dense + the new i18n_overlay_key_membership; byte-equivalence SHA `d9db528bea1c7a1e02835c4d3edb3fabcee3687aad2186608f1f1d2ac83b3b9b` (14 locales, 1,584,856 bytes) unchanged; regenerate + `git diff --exit-code` clean (reproducible); `npm run build` (client + admin) clean.
- Adversarial per-locale verify: all 13 non-English locales have exactly 1925 keys == `Leaves(en)`, 0 extra/typo'd, 0 missing (dense), all string-valued, flat (0 object openers); `en` still nested; each resolves byte-identical to the committed `i18n.resolved.generated.ts` slice via an independent unflatten/merge.
- Fix 1 (test added, commit `2c2066f`): `tests/i18n_overlay_key_membership.test.ts` - a PERMANENT subset guard that every overlay key is a member of `Leaves(en)`, with positive teeth tests that a typo'd / invented / misspelled dotted key is flagged. Closes the "a typo'd dotted key must fail tsc OR a test" invariant in a way that survives Phase 6 (the dense exact-equality check does not - sparse overlays legally omit keys, but must still never carry a non-`en` key).
- Fix 2 (comment correction, commit `c5f6738`): `scripts/i18n_flatten_locales.mjs` header + loader comments claimed the migration was "idempotent / safe to re-run"; it is not - re-running now throws (the locale files are flat, and `flatten` rejects their dotted top-level keys by the fail-loud contract). Comments corrected to "one-shot, throws on re-run by design." No behavior change.
- Deferred (non-blocking, already tracked in state.md): (1) the non-English slices of the shared content layers in `src/ui/i18n.en.ts` (e.g. `gameStringsEs`, non-en sub-keys of `shellStrings`/`hudStrings`/...) are now orphaned (the flat overlays no longer spread them); `en` still spreads its own `.en` slices so the module must stay - removal is Phase 4+ cleanup. (2) the dense key/non-empty test does not catch a wrong-but-non-empty value, but the byte-equivalence + reproducibility gates do (and the adversarial verifier confirmed every value byte-identical), so no coverage gap ships.
- SHA note (resolved during QA): a reviewer flagged the generated file's on-disk sha256 `554a0e6c..` (2,003,477 bytes) and git blob `90b4326..` as differing from the cited `d9db528..` / 1,584,856. Confirmed NOT a discrepancy: `d9db528..` is the canonical recursive-key-sorted serialization of the 14-locale `translations` (the documented gate, `i18n_resolved_hash.mjs`); `554a0e6c`/`90b4326` are sha256 / git-blob of the formatted `.ts` FILE. All three are stable; byte-equivalence holds.
- Islands deferral confirmed CORRECT (not a QA defect): `world_entity_i18n.ts` and `talent_i18n.ts` remain nested by design (Phase 4); cross-platform-sync confirmed their nested read paths (`tTalent`, `worldEntityText`) are unaffected and no matcher/parity surface drifted.

### Phase 4 QA - PASS (2026-06-16)
Verdict: PASS. Findings across 6 lanes: 0 BLOCKING, 1 SHOULD-FIX acted on (1 new test), the rest NICE-TO-HAVE (deferred, all explicitly out of scope or pre-existing). Stopping rule (byte-equivalence drift) never tripped - the resolved table is byte-identical, so no divergence was misclassified.
- Method: shared the 12-lane coverage Workflow + adversarial verify with Phase 2 QA (above). Deterministic gates run in the main loop; the dialect invariants independently re-proven in the main loop with an esbuild-bundled adversarial check (own merge, not the project's).
- Validation matrix, all green: `tsc --noEmit` clean; targeted suite 137/137; byte-equivalence SHA `d9db528bea1c7a1e02835c4d3edb3fabcee3687aad2186608f1f1d2ac83b3b9b` (14 locales, 1,584,856 bytes) unchanged; regenerate + `git diff --exit-code` clean; `npm run build` (client + admin) clean.
- Independent dialect proof (main loop, esbuild-bundled): es_ES = 72 divergence-only overlay keys, fr_CA = 37, en_CA = 3 - ZERO equal-to-base in any of the three (divergence-only holds). Fall-through: of the keys each dialect OMITS, es_ES 1853/1853 resolve to the es base (1815 of them where es diverges from en, 0 wrongly to en); fr_CA 1888/1888 to fr_FR (1834 base-divergent, 0 to en); en_CA omits -> en (base is en). Resolver order in `scripts/i18n_build.mjs` is data-driven via `DIALECT_BASE` + base-then-dialect `deepMerge` (no scattered locale-code branching). Casts: `world_entity_i18n.ts` is English-only (no dialect `{} as` cast; only benign local builder accumulators like `const mobs = {} as MobTranslations`); `talent_i18n.ts` aliases es_ES/fr_CA via a `satisfies Record<Exclude<...>, TalentLocaleText>` base record (no cast, no reassignment). No new `as any`/`@ts-ignore`/`!` fudge introduced.
- Fix (SHOULD-FIX, commit `e20700f`): `tests/i18n_dialect_resolution.test.ts` - the Phase 4 QA packet requires a test proving (a) a dialect overlay key OVERRIDES the base and (b) an OMITTED dialect key falls through to the BASE, not en directly when the base diverges from en. Two lanes raised this and both verifiers ruled STANDS (SHOULD-FIX): no committed test pinned the resolved-table base-then-overlay semantic - only the opaque byte SHA did, so a reversed/dropped base merge would have surfaced as a hash mismatch, not a readable "expected JcJ got PvP". New test asserts on the generated table, data-driven per dialect: every overlay key wins and diverges from base; every omitted key resolves to base and (where base diverges from en, >100 such keys) never to en; plus readable anchors (`es_ES`/`fr_CA` `realmTypes.pvp` == "JcJ" not "PvP"; `en_CA.classDetails.labels.armor` == "Armour").
- Deferred NICE-TO-HAVE (explicitly out of Phase 4 scope, documented): (1) talent `titleOverrides` es_ES/fr_CA remain full duplicated blocks (live, not byte-gated, no cast there; deduping them was out of the phase cadence). (2) The same orphaned `i18n.en.ts` shared-layer non-en slices noted in Phase 2 QA - pre-existing, not Phase 4 dead code. (3) Asymmetric dialect model (talent `localeText` deduped to aliases while `titleOverrides` stays full) is a future-maintenance trap but correct today. (4) `DIALECT_BASE` restates the dialect graph at build time without a single shared source object - acceptable; world_entity is English-only now and talent owns its own aliasing.
- Timing-artifact finding dismissed (not a defect): the p4-privsec lane flagged an "uncommitted es.ts sentinel" - that was this session's mutation-proof edit observed mid-flight while it was briefly applied; it was restored (explicit-path `git checkout` + regen) and the tree was re-verified clean (SHA `d9db528..`, no sentinel, generated artifact == HEAD) before any commit.

### Phase 5 QA - PASS (2026-06-16)
Verdict: PASS. 6 review lanes (independent re-derivation, dead-code/cleanup, force-mutation test-bite in an isolated worktree, qa-checklist, cross-platform-sync, privacy-security) + adversarial verify: 35 findings, 0 BLOCKING / 0 HIGH / 0 MEDIUM / 2 LOW / 33 INFO. Stopping rule (reproducibility / registry-in-sync must be green) never tripped. No code change required: every load-bearing property already has a test that empirically BITES (proven by mutation), so there was no SHOULD-FIX (unlike Phases 2/4, where one untested resolved-table semantic justified a new test).
- Deterministic gates (main loop, all green): `tsc --noEmit` clean; scanner regenerates `src/ui/i18n.status.json` byte-identically (`git diff --exit-code` clean) -> reproducible; resolved table byte-identical (SHA `d9db528..`); named suite `localization_fixes` + `localization_coverage` + `server_i18n` + `i18n_resolved_equivalence` + `i18n_status_registry` = 91/91; `npm run build` (client + admin) clean.
- Independent re-derivation (adversarial, esbuild-bundled witness, hash re-implemented inline with `node:crypto` - never importing the scanner/hash modules): re-derived state + `enHash` + `placeholders[]` + `srcHash` for all 2278 keys x 13 locales and diffed vs committed. ZERO divergences across 2278 keys / 2278 enHashes / 2278 placeholder arrays / 29614 state rows / 29571 srcHash rows. Universe = main 1925 + sim 93 + server 79 + admin 181 = 2278; pending=0; blocked rows only in server(5)/admin(38)=43; `blockedSource`=105; every translated `srcHash`==`enHash`; en_CA all-translated (English dialect, by design). Allow-list views == seed == the deleted literal Sets EXACTLY (COPIED_ALLOW 43, ALLOW_V07_SLASH 105), proven by set-equality against `git show f4d0142:tests/localization_fixes.test.ts`.
- Force-mutation (isolated worktree, every mutation reverted, registry byte-identical after each reset): all five required mutations made the test they target BITE, with real failure messages captured. (1) drop a key -> "missing rows for: main:a11y.characterActions"; add a phantom key -> "rows with no source key: zzz:bogus". (3) English text change moved `enHash` (independently reproduced via `contentHash`). (4) placeholder rename `{name}`->`{playerName}` moved BOTH `enHash` AND `placeholders[]` (proves the M1c placeholder-parity guard cannot regress silently). (5) de-cognating a blocked row (`server::fr_FR::who.statusCombat` "combat"->"en plein combat") made the blocked-load-bearing test bite ("blocked but no longer copies English"). Determinism / no-network / no-LLM confirmed; no `Date.now`/`Math.random` in any scan script; `src/sim`+`server`+`net`+`render`+`headless`+`python` import nothing from the scanner/hash/seed/registry (build/test-only, three-host/IWorld parity N/A).
- Deferred LOW (no action this phase, both already mitigated by existing permanent guards; revisit in Phase 6): (1) The QA doc's STEP-2 claim that "the registry-in-sync test fails if an overlay holds a key absent from en" is INACCURATE - it does not, because the scanner enumerates `main` rows by `en` leaves only (the en-driven universe documented in state.md). That property IS guarded, by the Phase 3 `tests/i18n_overlay_key_membership.test.ts` (+ `i18n_flat_overlay_dense.test.ts`), both of which bit in the mutation run. Correct guard, wrong test - a doc-attribution nuance, not a coverage gap. (2) The scanner's seed-rot `throw` (blocked seed key not in the universe) and the `pending` branch are only INDIRECTLY asserted - the throw is hard-gated by the scanner running in `pretest`/`build` (any seed rot fails the gate), and `pending` is genuinely empty while dense, so there is nothing to positively assert until Phase 6 makes sparseness live. Dedicated positive assertions are a natural Phase 6 add (pending becomes real there).
- Tree hygiene: the force-mutation lane's isolated worktree auto-provisioned at an unrelated base, so the agent recreated it at HEAD `689acad` on scratch branch `qa-phase5-mutation`; the worktree was removed in cleanup. The harmless local scratch branch (same commit as HEAD) was left in place after `git branch -D` was declined by policy - delete at will.

### Phase 6 QA - PASS WITH FIXES (2026-06-16)
Verdict: PASS WITH FIXES. 6 coverage lanes (correctness, test-coverage, dead-code/cleanup, privacy-security-review [MANDATORY], cross-platform-sync, qa-checklist) + direct main-loop reproduction of every gate case + the mandatory privacy lane re-run: 0 BLOCKING / 0 HIGH / 0 MEDIUM-left, 1 MEDIUM fixed, 2 LOW deferred, rest INFO. Stopping rules (any gate case un-fixable; pending-English could reach a user) never tripped. One fix landed (commit `79d0732`): the empty-pending release gate now has an asserted teeth test.
- Method: the four gate cases were RE-PROVEN directly in the main loop (not trusted from the implementer's report), then a 6-lane adversarial-verify Workflow audited the whole Phase 6 diff (`30e070e..HEAD`) in coverage mode, then the mandatory privacy-security lane was re-run standalone (the first workflow run of it did not return a structured verdict).
- Validation matrix, all green: `npx tsc --noEmit` clean; resolved-table hash recomputes to the committed baseline `d9db528bea1c7a1e02835c4d3edb3fabcee3687aad2186608f1f1d2ac83b3b9b` (14 locales, 1,584,856 bytes) - UNCHANGED is correct (overlays dense => `pending` empty => byte-identical; STEP-0 paper-trail confirmed in state.md row 6 + commit `ba88198`, no unexplained move to flag); generated `pending` = 0 keys across 13 locales; PR tier full suite 1284 passed / 7 skipped (1291); release tier 1291 passed / 0 skipped; `npm run build:env` + `build:server` + full client+admin `build` clean and reproducible (git tree clean after each); main bundle gzip 1,121.01 KB (unchanged vs Phase 2's 1,120.64, no balloon).
- The four gate cases, observed results (each reproduced directly):
  1. UNTRACKED key THROWS in dev/test - `t("totally.bogus.untracked.key")` throws `/untracked key/` (i18n_t_behavior passes); and the same literal FAILS the PR gate via tsc `TS2345` (rejected against the finite 1648+-member `TranslationKey` union, scratch-file proof, tree restored).
  2. PENDING key renders English on NON-RELEASE only - the doMock synthetic-key test renders the English fill on non-release and `/pending/`-throws on release; both pass.
  3. PENDING key BLOCKS the release gate - dropping a real translation (`es."nav.home"`) made `pending.es=["nav.home"]`, English-filled `es.nav.home="Home"`, moved the hash to `524d95f4..`, and FAILED 6 release-tier gates (2x empty-pending + byte-equivalence + `--check` + 2x reproducibility); tree restored to `d9db528..`, git clean.
  4. UNREGISTERED+untranslated key FAILS the PR gate - tsc `TS2345` for `t()` literals; `s3_registered` (en, locale-independent recognition) for matcher emits. Both biting.
  Plus: tsc English-completeness via the dense `: typeof en` chain is live - removing `nav.home` from `en` produced 28 `TS2353` errors across all 13 overlays; restored clean. And the content checks REALLY moved release-only: the 7 PR-skipped/release-run tests are exactly 2x empty-pending, 2x quest-narrative, 2x H3b copied-English, s3_localized.
- Fix (MEDIUM -> commit `79d0732`, `test(i18n):`): `tests/i18n_t_behavior.test.ts` - the release-tier "committed pending set is empty" check only ever ran against the REAL (empty) data, so it proved today's data clean but never that the assertion would CATCH a non-empty set (a weakened assertion, or `pending` collapsed to `{}`, would still pass). Factored the gate check into one shared `assertNoPending()` helper called by both the real-data check (now with a non-vacuous locale-count floor) and a NEW teeth test that feeds a synthetic non-empty pending map and asserts it FAILS, plus a clean-map false-positive guard. Proven biting by mutation: weakening the helper to a tautology fails the teeth test. Teeth run at both tiers; the real empty-pending stays release-only.
- privacy-security-review (MANDATORY): NON-BLOCKING, all 3 sub-checks PASS. (1) `t()` throws the pending key at L162-165 BEFORE `return interpolate` at L167 on a release build; no `?? 'English'`/`|| 'English'`/default-param/const-map fallback on any miss path. (2) `isReleaseBuild()` is fail-closed for the deploy path: `vite build` statically replaces `import.meta.env.PROD`=true; `vite.config.ts` define block does not touch it; the server esbuild bundle never imports the client `t()`. (3) No new leak path - `pending` is imported only by `i18n.ts`, the sim/server/admin matchers render from their OWN self-contained tables independent of `translations`/`pending`, and index.html `data-i18n` keys are pinned to real `en` leaves.
- cross-platform-sync: NON-BLOCKING. The S3 split preserves the full `sim.ts`/`hud.ts` source-scraping + arm-matcher coverage (only delta: factor the shared candidate loop into `candidateStrings()` + add `localizedOut()`); `s3_registered` (en) still catches an unregistered emit because recognition is locale-independent. `src/sim`/`server` UNCHANGED in range (no `t()`/DOM/`Math.random`/`Date.now` added; neither imports the build/scan scripts). build `computePending` and scan `providedByLang` produce identical pending sets on dense/sparse/dialect inputs.
- Deferred (non-blocking, recorded for later; QA chose discipline over scope-creep):
  1. LOW (correctness): `workflow_dispatch` on a `release/**` ref routes to `pr-gate`, never `release-gate` (`ci.yml:37,70`) - so manual dispatch cannot trigger the full 14-locale validation. This MATCHES the documented intent in the ci.yml comment ("manual dispatch -> pr-gate"), so it is correct-as-designed, not a defect; left unchanged. MAINTAINER DECISION: if manual release validation is wanted, add `|| (github.event_name == 'workflow_dispatch' && startsWith(github.ref, 'refs/heads/release/'))` to the release-gate `if` and exclude that case from pr-gate.
  2. LOW (dead-code + qa-checklist): stale references to the deleted `tests/i18n_flat_overlay_dense.test.ts` (and pre-Phase-6 "still DENSE" wording) linger in the DORMANT one-shot migration script `scripts/i18n_flatten_locales.mjs` (lines ~9, ~15, and the `header()` emitter ~71). Outside the Phase 6 diff range; cosmetic (the script throws on re-run, so `header()` never emits); the LIVE overlays already carry the correct `i18n_overlay_key_membership.test.ts` comment. Trivially fixable later; not worth shared-worktree churn now.
- Privacy residual carried to Phase 7/8 (not blocking): production release semantics rest SOLELY on `import.meta.env.PROD` being statically true under `vite build`; no explicit `I18N_RELEASE=1` in any deploy/Docker/CI env. Dormant today (`PENDING_TOTAL===0`). Before the first real pending key ships (Phase 7), set `I18N_RELEASE=1` in the prod build env as defense-in-depth against a future non-Vite/SSR/Node client bundling path that would evaluate release=false and English-fill a pending key.
- INFO confirmations (no action): all four locked decisions verified PASS; the untracked-throw, pending-doMock, s3 split, and index.html-keys tests are each non-vacuous (sanity floors prove the candidate lists are populated); the build-pending vs registry-pending cross-check is dormant-but-correct while dense (a natural Phase 7 add when pending goes live).

### Phase 7 QA - PASS WITH FIXES (2026-06-16)
Verdict: PASS WITH FIXES. Method: every load-bearing stopping-rule proof run DIRECTLY in the main loop (not trusted from the implementer), then a 6-lane read-only coverage Workflow (correctness, test-coverage, dead-code/cleanup, privacy-security-review, cross-platform-sync, qa-checklist). Across all lanes + main-loop proofs: 0 BLOCKING, 2 HIGH (both fixed), 2 MEDIUM (both fixed), 3 LOW (1 fixed, 2 deferred), rest INFO. The two STOPPING RULES (tool non-deterministic; prose could route to auto-fill) never tripped.
- Main-loop proofs, all green: DATA-ONLY (grep of the tool + its imports: no fetch/http/net/dns/SDK/LLM token; the only dynamic `import()` is a locally-esbuilt `data:` URL). DETERMINISTIC NO-OP (`i18n:worklist` x2 on the unchanged repo -> byte-identical, cache hit `25e8af71`; non-empty batch -> byte-identical `54d42499`). ROUND-TRIP (depopulate de_DE 1 chrome `loading.worldProgress` w/ `{done}`/`{total}` + 2 prose `classes.mage`/`entities.quests.q_wolves.title` -> scan -> worklist routes the chrome key to autoFillable with english+placeholders+6 `loading.*` siblings+glossary, BOTH prose keys to humanRequired and ABSENT from autoFillable; fill the chrome key -> pending 3->2; full revert -> tree clean, worklist back to `25e8af71`). STOPPING RULE across the FULL 1925-key en space (0 keys are both fillable AND prose-prefixed; no narrative hides under a chrome namespace - the 2 longest chrome values `game.prestige.body`/`hud.arena.queueNote` are UI mechanics copy, correctly auto-fillable). GLOSSARY ships in every batch (verbatim brand + 135 resolved terms).
- Validation matrix, all green: `npx tsc --noEmit` clean; named suite `i18n_fill_worklist` (now 14) + `localization_fixes` + `localization_coverage` + `server_i18n` = 83 passed / 5 skipped (the 5 are the pre-existing release-tier gates at the PR tier); `npm run build` (client + admin) clean, main bundle gzip 1,121.01 KB (no balloon); generated files (resolved table, status.json, manifest) regenerate byte-identical (git clean). Resolved-table baseline UNTOUCHED (`d9db528..`; QA changed no overlay data).
- Fixes landed:
  - HIGH x2 (test-coverage, Lane B) -> commit `test(i18n): cover worklist determinism and blocked-prose segregation`: the QA spec's two named acceptance tests had NO code coverage - (a) byte-identical no-op/determinism and (b) end-to-end prose->humanRequired routing + the belt-and-suspenders throw were proven only by the manual round-trip (the unit tests only exercised classify/siblingKeys/expandGlossaryTerms). Extracted a PURE assembly core `buildWorklistOutputs` + `assertAutoFillableHasNoProse` from `scripts/i18n_fill_worklist.mjs` (byte-identical output reproven: empty `25e8af71`, non-empty `54d42499` unchanged) and added 4 fixture-driven tests: deterministic `fileEntries`+`inputHash` with an inputHash-moves-on-change teeth check; a pending prose key routes to humanRequired and is ABSENT from autoFillable end to end (with a fully-translated language yielding no batch); glossary-in-every-batch + dialect `resolveLocaleValue` (own overlay -> English fallthrough); and the throw bites on a forced prose entry without false-positiving on chrome.
  - MEDIUM (cross-platform-sync, Lane E) -> commit `docs(i18n): correct sim/server matcher guidance and worklist caveats`: the new Phase 7 matcher guidance ("register a RULE in sim_i18n.ts AND its server_i18n.ts mirror in the SAME change") overstated the S3 guard, which accepts recognition by EITHER matcher (verified at `tests/localization_fixes.test.ts:563` - `localizeServerText(s)!==null || localizeSimText(s)!==null`; the leak message names sim_i18n only). Reworded both `docs/i18n-scaling/translation-workflow.md` + `src/ui/CLAUDE.md` step 2 to ORIGIN-based registration (sim_i18n for a `src/sim/` emit, server_i18n for a `server/` emit; the guard fails only if neither recognizes it). The guidance over-specified, never under-specified, so it could not have shipped silent English - a doc-accuracy fix.
  - MEDIUM (correctness, Lane A) -> same docs commit: the glossary `verbatim` brand ("World of ClaudeCraft") also lives inside auto-fillable `footer.*` chrome and is shipped as REFERENCE only (not enforced); a bot ignoring the glossary could mistranslate it with no test catching it. Defense-in-depth, not a stopping-rule break (prose still cannot reach autoFillable). Added an ADVISORY note to translation-workflow.md (the glossary is reference; the fill owner spot-checks brand preservation until an automated guard exists). Deferred the automated brand-preservation guard to the fill pipeline (Phase 8+).
  - LOW (dead-code, Lane C) -> same docs commit: one em dash (U+2014) introduced by Phase 7 in the `src/ui/CLAUDE.md` i18n heading -> ASCII hyphen. (The other ~16 em/en dashes in that file are pre-existing house style OUTSIDE the Phase 7 diff; left untouched.)
  - LOW (correctness, Lane A) -> same docs commit: a scoped `--lang` run silently deletes other languages' batches + writes a partial manifest; added a one-line caveat to translation-workflow.md (a scoped run replaces the dir; do a full run before relying on it for a release).
- Deferred (non-blocking, recorded):
  - LOW (Lane A/E): `DIALECT_BASE`/`isPresent`/`sourceModule`/DICT-export wiring are hand-duplicated across `i18n_fill_worklist.mjs` + `i18n_build.mjs` + `i18n_scan.mjs` (verified byte-identical today). Extracting a shared constants module (or a deep-equal lockstep test) touches the Phase 2/5 build+scan scripts (out of Phase 7 scope); already tracked as deferred INFO (Phase 6 QA). Revisit when the shared-constants extraction lands.
  - INFO (Lane B): the classify() unrecognised-namespace anchors (worldContent/wiki/news/comingSoon) are synthetic, not real flat keys - the assertion is valid (pure string logic) but does not double as a real-key witness. Cosmetic; no action.
  - INFO (Lane F): a full-partition classification test (every en main key is prose XOR chrome XOR unclassified, with an expected unclassified count) is optional Phase 8 hardening.
- Reviews: privacy-security-review PASS (data-only / no-exfiltration / no-secrets / gitignored output confirmed end to end; the batch + manifest embed only English source, placeholders, sibling English, glossary terms, and hashes - no translator identity, no absolute paths); cross-platform-sync PASS_WITH_FINDINGS (resolution constants byte-identical to build + scanner; sim/server language-agnostic intact; the one matcher-doc finding fixed); qa-checklist PASS_WITH_FINDINGS (every STEP-5 acceptance item satisfied; LOW/INFO hardening only).
Commits: `test(i18n): cover worklist determinism and blocked-prose segregation`, `docs(i18n): correct sim/server matcher guidance and worklist caveats`, this doc commit.
OPEN carried forward: RFC 9.6 release-fill ownership / API key still UNRESOLVED (recorded in `translation-workflow.md` + `state.md`). Handoff: Phase 8 (admin catalog into the model).

### Phase 8 QA - PASS WITH FIXES (2026-06-16)
Verdict: PASS WITH FIXES. Method: every load-bearing claim re-proven DIRECTLY in the main loop (not trusted from the implementer), then a 6-lane coverage Workflow (correctness, test-coverage, dead-code/cleanup, privacy-security-review [MANDATORY], cross-platform-sync, qa-checklist) with BLOCKING/HIGH findings adversarially verified. Across all lanes + main-loop proofs: **0 BLOCKING / 0 HIGH**; a few MEDIUM/LOW (2 acted on, the rest deferred with rationale). The two STOPPING RULES (admin 14-locale completeness regresses; a non-client surface leaks pending-English to a real user at release) NEVER tripped.
- Main-loop deterministic gates, all green: `npx tsc --noEmit` clean; PR-tier named suite (`localization_fixes` + `localization_coverage` + `server_i18n` + `i18n_admin_catalog` + `i18n_status_registry`) 102 passed / 6 skipped; `npm run build` (game + admin) clean - admin is a SEPARATE chunk `dist/assets/admin-*.js` 175 KB / 33 KB gzip, game main bundle gzip 1,121 KB (no balloon); reproducibility EXACT (`npm run i18n:admin && i18n:scan` -> `git status --porcelain` empty on both generated files); game byte-equivalence baseline recomputes to `d9db528bea1c7a1e02835c4d3edb3fabcee3687aad2186608f1f1d2ac83b3b9b` (1,584,856 B) UNCHANGED; built admin chunk has 0 matches for `abilityUi`/`classDetails`/`entities.zones`.
- STOPPING-RULE (a) - completeness regression PROVEN ABSENT: an independent esbuild diff of the pre-Phase-8 dense DICT (`git show 77a15ac:src/admin/i18n.ts`) vs the new resolved table found 0 locales dropped, **0 of 181x14 existing (locale,key) pairs with a changed/lost value**, 0 resolution gaps across all 14 locales x 219 keys, and exactly 38 new keys added (the chat-filter/chat-moderation surface). The 181 existing registry rows are byte-identical (0 state flips; 38 blocked cognates preserved).
- STOPPING-RULE (b) - non-client leak PROVEN ABSENT + the two-tier gate has REAL teeth: the RELEASE tier (`I18N_RELEASE_TIER=1`) fails on EXACTLY the 2 intended checks with no collateral - `i18n_status_registry` empty-pending (expected 0, got 456 = 38 new keys x 12 non-en/non-en_CA locales) and `localization_fixes` H3b copied-English (`admin::es::nav.chatFilter`) - while 106 tests pass (all 181 existing admin rows + every main/sim/server row green). RFC 9.7 audit holds: admin.html 18/18 `data-i18n` keys are real admin en keys (so `localizeStatic` can't throw-on-untracked and the release gate forces them filled before shipping); index.html `data-i18n-content` meta is `seo.title`/`seo.description` (game-side, game pending=0 at release); 15 hreflang links are language-routing URLs. admin `t()` hard-fails a pending key at release as a never-fires backstop (verified live: I18N_RELEASE=1 + es + nav.chatFilter THROWS).
- Reviews: privacy-security-review (MANDATORY) PASS - auth/route/token/`is_admin` guards byte-identical, escapeHtml counts unchanged (tables.ts 33->33, main.ts 4->4), every player-controlled interpolated value escaped at the sink, release `t()` throws before `interpolate`, no secret/PII in new strings/table/worklist. cross-platform-sync PASS - `src/sim/`+`server/` empty diff, matchers + `world_api.ts` + `online.ts` byte-identical (blob SHAs unchanged), registry diff admin-only (+38 keys / +494 rows, pending 0->456), `DIALECT_BASE` identical across all 3 build scripts. correctness / test-coverage / dead-code / qa-checklist all PASS_WITH_FINDINGS (no blocker). No Postgres/DDL/persisted-state change (28-file diff has zero `server/`/`db.ts`/SQL).
- Fixes landed (2 commits, explicit paths):
  - `test(admin): recurse into i18n.locales when checking bundle isolation` (`b9cc7cd`): the bundle-isolation test used a non-recursive `readdir` over `src/admin`, so a game-table import smuggled into a per-locale overlay would have escaped the no-`..`-import check. Now walks the whole admin tree + a sanity floor proving the overlays are scanned. Mutation-PROVEN to bite: a resolvable `from "../../ui/i18n.resolved.generated"` planted in an overlay trips the offenders assertion (it did not before).
  - `docs(i18n): correct stale scanner header after admin sparseness` (`7b3e195`): `scripts/i18n_scan.mjs` header still said "the admin DICT keys" and "pending ... EMPTY this phase: everything is still dense" after Phase 8 moved admin to a sparse `addSparseScope` with 456 pending rows. Comment-only; registry regenerates byte-identical (verified). state.md additions-log row 58 inventory line + the Active-phase headline were also refreshed (this doc commit).
- Deferred (non-blocking, recorded with rationale):
  - LOW/cosmetic (correctness + qa-checklist, CONVERGED): admin date formatters (`src/admin/format.ts:27` `fmtDate` + the four `dialog.until` rows in `main.ts:333,362,384,409`) render via Intl but key off the HOST default locale, not the operator's selected admin locale. PRE-EXISTING pattern (`fmtDate` already did this; 2 of the 4 dialog sites predate Phase 8) and NOT an i18n-rule violation (dates still go through Intl, no English literal escapes). Optional follow-up: thread `adminLanguage()` into the formatters. Out of Phase 8 scope.
  - MEDIUM (test-coverage): no test proves the localized hardcoded admin strings (main.ts alerts, tables.ts title/placeholder) route through `t()` - there is no admin equivalent of the game-side `hud.ts` source-scan. Confirmed done by grep (all 8 `window.alert` + `document.title` route through `t()`/`localizeAdminError`), but unguarded against a future re-introduced literal. DEFERRED to Phase 9: the `en_XA` pseudo-locale is purpose-built to surface hard-coded literals that never became `t()` keys (admin included), which is the right backstop for this gap.
  - LOW/INFO: `adminLanguage()` getter is unused (kept as public-API symmetry with the used `setAdminLanguage`); H3b copied-English release gate short-circuits on the first offender (diagnostics quality; the registry pending-count gate independently covers the full set); the worklist admin-pending path is covered by the live tool run + `classify()` unit but not the `buildWorklistOutputs` fixture (optional fixture add); `src/admin/CLAUDE.md` attributes `reasonLabel` to `i18n.ts` but it lives in `tables.ts` (pre-existing doc nit); migration script exits 0 (not non-zero) on its refuse-to-re-run path (benign for a manual tool).
Commits: `b9cc7cd` (test fix), `7b3e195` (scanner comment fix), this doc commit. Handoff: Phase 9 (`docs/i18n-scaling/phase-09-pseudo-locale.md`, optional pseudo-locale) - or declare the packet complete if stopping here.

### Phase 9 QA + whole-packet QA - PASS-WITH-FOLLOWUPS (2026-06-16)
Verdict: PASS-WITH-FOLLOWUPS. This is the FINAL whole-packet QA. Method: every load-bearing gate
re-run DIRECTLY in the main loop (not trusted from the implementer), then a 6-lane read-only
coverage Workflow (correctness, test-coverage, dead-code/cleanup, privacy-security-review,
cross-platform-sync, qa-checklist) with the one HIGH finding adversarially verified. Across all
lanes + main-loop proofs: **0 BLOCKING / 1 HIGH (adversarially downgraded to MEDIUM; FIXED) / 0
other MEDIUM / 4 LOW / 15 INFO.** Neither stopping rule tripped (no whole-feature invariant
failed; en_XA never leaks into supportedLanguages, hreflang, the release gate, or the player /
operator picker - all four exclusions re-verified directly in source).
- Deterministic gates, all as expected: `npx tsc --noEmit` clean; PR tier `npm test` = 131 files,
  **1331 passed / 7 skipped** (= prior 1330 + this QA's new index.html-absence assertion; the 7
  skips are the pre-existing release-tier-only gates); release tier (`I18N_RELEASE_TIER=1`) fails
  on EXACTLY the 2 intended Phase 8 checks (`i18n_status_registry` empty-pending = 456, and the
  admin H3b copied-English) with ZERO collateral and ZERO Phase 9 regression - this is the
  two-tier gate correctly demanding the deferred 14-locale admin fill, not a defect; `npm run
  build:env` + `build:server` + `build` (game + admin) all exit 0; reproducibility EXACT
  (`git status --porcelain` clean after both the pretest regen and the full build - both resolved
  tables + `i18n.status.json` + media manifest regenerate byte-identical); game byte-equivalence
  baseline UNCHANGED (`d9db528..`, 1,584,856 B - the hash reads `supportedLanguages` only, which
  omits en_XA); bundle NOT regressed (main 1,121.05 KB / admin 33.17 KB gzip, byte-identical to
  state.md); copy review clean (no em dashes / emojis introduced in any shipped string or doc).
- en_XA PROD tree-shake PROVEN EMPIRICALLY (not by reading): 0 pseudo-only glyphs (`ɱ ʋ ɋ ẋ ĝ ĥ`,
  used only by the accent map, never by the 14 real locales) in `dist/assets/main-*.js` +
  `admin-*.js`. The pseudo table is dead-code-eliminated from both production bundles; the lone
  surviving `en_XA` token is the `DEV_PSEUDO_LOCALE` string constant, not table data.
- The 6 lanes: correctness PASS; test-coverage PASS_WITH_FINDINGS (the one HIGH below);
  dead-code PASS_WITH_FINDINGS (shared transform genuinely shared via `scripts/i18n_pseudo.mjs`;
  every en_XA symbol reachable; no scaffolding); privacy-security-review PASS (FAIL-CLOSED: the
  only writer of `pseudoActive` is the URL selector, gated by `!isReleaseBuild()` covering both
  prod signals; two independent locks with the `tableFor` PROD guard; en_XA never persisted,
  unselectable via setLanguage/setAdminLanguage; no secret/PII/XSS/auth surface touched);
  cross-platform-sync PASS (`src/sim/`, `server/`, `headless/`, `python/`, `net/`, `render/`,
  `game/`, `world_api.ts` all untouched by the diff; matchers unchanged; offline Sim and online
  ClientWorld render through the same `t()`); qa-checklist PASS_WITH_FINDINGS.
- Fix landed (1 commit, explicit path) - `test(i18n): assert en_XA absent from index.html
  (hreflang + picker)` (`590d7b3`): the HIGH finding. The Phase 9 pseudo test file's header
  CLAIMED hreflang exclusion coverage but only asserted the supportedLanguages / isSupportedLanguage
  / admin-DICT exclusions; the `localization_coverage` hreflang test is PRESENCE-ONLY, so a future
  hand-edit leaking `<link hreflang="en-XA">` or an `en_XA <option>` into index.html would pass CI.
  Added a whole-file, case-insensitive assertion that index.html references neither `en_XA` nor
  `en-XA` (guards both the hreflang block AND the static language picker at once), making the
  header claim true. index.html is clean today, so this is regression-prevention, not a live leak.
- Deferred (non-blocking, recorded; QA chose discipline over scope-creep, per prior phases):
  - LOW (correctness / dead-code / qa-checklist, CONVERGED): admin `tableFor()` (`src/admin/i18n.ts:105`)
    omits the game's `lang === current` guard (`src/ui/i18n.ts:182`). BENIGN today - admin `t()` is
    the sole caller and always passes `current` (no admin per-lang read path exists), so the looser
    predicate is unreachable as a bug. Latent footgun only if a future admin feature reads a
    non-current locale while `pseudoActive`. Optional one-line hardening: mirror the game guard.
  - INFO: `tableFor` gates the pseudo return on `!import.meta.env.PROD` while the selector gates
    `pseudoActive` on the stronger `!isReleaseBuild()`. This is INTENTIONAL and correct: only the
    literal `import.meta.env.PROD` is statically replaced by Rollup, so it (not the `isReleaseBuild()`
    function call) is what dead-code-eliminates the en_XA reference; the selector's `!isReleaseBuild()`
    is the real runtime gate and covers the dev + `I18N_RELEASE=1` case. Empirically confirmed by the
    glyph-free dist.
  - INFO (doc accuracy, optional): under en_XA the sim/server matcher MESSAGE TEMPLATES stay English
    (getLanguage() = "en"), but keyed ENTITY names spliced into them (item/mob/zone via `tEntity` ->
    `tableFor`) DO render pseudo-localized - correct, since those are real `t()` keys. The phase doc's
    "matcher channel stays English" note is therefore partial; refine if revisited.
  - INFO: leaf-count thresholds in the pseudo test (`>1000` / `>100`) are loose lower bounds; the
    per-path parity test already enforces no missing leaves, so coverage is fine.
- Whole-feature QA matrix (`docs/i18n-scaling/qa-checklist.md`), per row: the invariant ("a key
  neither translated nor registered fails the PR gate; a merely-`pending` key cannot survive a cut
  release") HOLDS at both tiers - demonstrated live by the 456 admin pending rows (legal at PR,
  blocking at release). Completeness & safety: PASS. Two-tier gate: PASS (PR green, release blocks
  incomplete, ci.yml ref-routing proven in Phase 6). Sim/server boundary: PASS (untouched).
  Determinism: PASS (pure fixed map, no RNG/clock). Persistence: PASS (no DDL/JSONB change).
  Performance/bundle: PASS (gzip unchanged; `asset:budget`/`perf:tour` N/A - zero render/asset
  change). Copy & a11y: PASS (no em dashes/emojis; `hud.ts:1674` `Math.ceil` cooldown bypass noted
  PRE-EXISTING). Non-client surfaces (RFC 9.7): PASS (re-confirmed Phase 8 + en_XA exclusion).
  Build gate: PASS. Contributor experience: PASS (add-key-to-en flow + worklist no-op proven Phases
  6/7). Deploy verification: N/A (not deployed in this pass).

OPEN carried forward (surface before teardown): the en_XA-surfaced hard-coded literals (Phase 9
section above), the OPEN RFC 9.6 release-fill ownership / API-key item, and the admin chat-filter
14-locale fill (456 pending rows) the release gate is correctly holding.
Commits: `590d7b3` (hreflang/picker absence test), this doc commit.

## Notes (per phase, post-completion)
- Phase 0: pure rename, verified byte-identical resolved table (SHA-256), 73 localization tests green. Prerequisite readability step, already on the branch.
- Phase 5: scanner is a PURE function of source (en + sim/server/admin DICTs + the static seed) - it deliberately does NOT read its own prior output, so reproducibility holds from-scratch AND incrementally. Consequence: `srcHash` is recomputed from current English each run, so it always equals the per-key `enHash` this phase and the translated/stale split is DORMANT (pending stays empty while dense). The `srcHash`/`enHash` fields are recorded now so the staleness comparison (`srcHash !== enHash` -> pending) can go live in Phase 6+ once sparse overlays + the fill pipeline persist translation-time hashes. Two helper scripts added beyond the doc's `i18n_scan.mjs`: `scripts/i18n_hash.mjs` (side-effect-free content hash, shared with the registry test) and `scripts/i18n_blocked_seed.mjs` (canonical blocked-surface seed). Reviewed by privacy-security-review + cross-platform-sync + an adversarial correctness pass: 0 BLOCKING. cross-platform-sync confirmed the HUD-local matcher maps (`localizeErrorText`/`localizeSystemText`/`localizeLootText`) are correctly NOT registry rows - their target `t()` keys live in the `main` scope (translation axis) and their recognition is still guarded by the S3 source-scraper (recognition axis); split-axis by design, not a gap.
