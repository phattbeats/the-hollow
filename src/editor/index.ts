// Public surface of the map-editor subsystem (the /editor Vite entry). Only
// main.ts and tests should reach into src/editor/ from outside this directory;
// everything else here is an internal implementation detail of EditorApp.

export { EditorApp } from './app';
